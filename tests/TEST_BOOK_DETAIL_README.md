# Тесты для book-detail.js

Этот набор тестов проверяет функциональность страницы детальной информации о книге (`book-detail.js`).

## 📁 Файлы тестов

1. **`test_book_detail.html`** - Интерактивный HTML-тест для проверки функционала в браузере
2. **`test_book_detail_api.py`** - Python-тест для проверки API эндпоинтов

## 🚀 Запуск тестов

### HTML-тесты (в браузере)

1. Убедитесь, что сервис каталога запущен на порту 8000:
   ```bash
   # Запуск сервиса каталога
   cd services/catalog
   python -m uvicorn main:app --host 127.0.0.1 --port 8000
   ```

2. Откройте файл `tests/test_book_detail.html` в браузере

3. Нажмите кнопки для запуска различных тестов:
   - **Проверка API эндпоинта** - проверяет доступность API
   - **Тест парсинга URL** - проверяет извлечение ID из URL
   - **Тест создания карточки** - проверяет создание DOM-структуры
   - **Тест обработки ошибок** - проверяет обработку ошибок
   - **Тест очистки main элемента** - проверяет очистку контейнера
   - **Полный интеграционный тест** - проверяет весь цикл работы

4. Используйте раздел "Демонстрация работы" для визуальной проверки

### Python-тесты (через командную строку)

1. Установите зависимости (если еще не установлены):
   ```bash
   pip install requests
   ```

2. Убедитесь, что сервис каталога запущен на порту 8000

3. Запустите тесты:
   ```bash
   python tests/test_book_detail_api.py
   ```

   Или:
   ```bash
   cd tests
   python test_book_detail_api.py
   ```

## 📋 Что проверяют тесты

### HTML-тесты (`test_book_detail.html`)

1. **Проверка API эндпоинта**
   - Доступность API на `http://localhost:8000/api/v1/audiobooks/{id}`
   - Корректность ответа
   - Отображение полученных данных

2. **Тест получения ID из URL**
   - Парсинг параметра `?id=...`
   - Обработка различных форматов URL
   - Обработка отсутствующего ID

3. **Тест создания карточки книги**
   - Создание всех элементов DOM
   - Правильность структуры карточки
   - Наличие всех обязательных элементов (заголовок, автор, описание, обложка)
   - Корректность атрибутов (data-product-id и т.д.)

4. **Тест обработки ошибок**
   - Отображение ошибок через `showError()`
   - Обработка отсутствующего ID
   - Обработка ошибок API

5. **Тест очистки main элемента**
   - Полная очистка содержимого перед добавлением карточки

6. **Полный интеграционный тест**
   - Весь цикл: получение ID → запрос к API → очистка → создание карточки → добавление в DOM

7. **Демонстрация работы**
   - Визуальная демонстрация загрузки и отображения реальной книги

### Python-тесты (`test_book_detail_api.py`)

1. **Доступность API** - проверка, что сервис отвечает
2. **Получение книги по ID** - проверка получения конкретной книги
3. **Обработка несуществующей книги** - проверка корректного возврата 404
4. **Структура ответа** - проверка наличия всех необходимых полей
5. **Получение списка книг** - проверка эндпоинта списка всех книг

## ✅ Ожидаемые результаты

Все тесты должны пройти успешно, если:
- Сервис каталога запущен на порту 8000
- В базе данных есть хотя бы одна книга с ID=1
- API корректно работает

## 🔧 Устранение проблем

### Ошибка подключения к API

```
❌ Не удалось подключиться к API
```

**Решение:** Убедитесь, что сервис каталога запущен:
```bash
cd services/catalog
python -m uvicorn main:app --host 127.0.0.1 --port 8000
```

### Книга с ID не найдена

```
⚠️ Книга с ID=1 не найдена
```

**Решение:** Создайте тестовые данные или измените ID на существующий в базе данных.

### CORS ошибки

Если возникают CORS ошибки при запросах из браузера, убедитесь, что в сервисе каталога настроен CORS middleware.

## 📝 Примечания

- HTML-тесты используют реальный API, поэтому сервис должен быть запущен
- Python-тесты проверяют только API, без JavaScript
- Для полной проверки рекомендуется запустить оба типа тестов

