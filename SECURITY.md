# 🔐 Безопасность и конфиденциальные данные

## ⚠️ ВАЖНО: Защита секретных данных

Этот проект содержит конфиденциальную информацию, которая **НЕ должна** попадать в систему контроля версий (Git).

## 📋 Файлы с секретными данными

Следующие файлы содержат секретные данные и **уже добавлены в .gitignore**:

- `config.env` - основной файл с секретами (токены, пароли, API ключи)
- `.env` - альтернативный файл конфигурации
- `*.env` - любые файлы с расширением .env
- `*.db` - файлы баз данных

## 🚀 Настройка для нового окружения

### Шаг 1: Создайте config.env

Скопируйте пример конфигурации:

```bash
cp config.env.example config.env
```

### Шаг 2: Заполните секретные данные

Откройте `config.env` и заполните следующие поля:

```env
# API Keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# JWT Security
SECRET_KEY=your-super-secret-key-that-is-long-and-random-CHANGE-IN-PRODUCTION

# Telegram Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# Email Configuration (SMTP)
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password_here
EMAIL_FROM=your_email@gmail.com
EMAIL_COPY_TO=your_email@gmail.com

# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_admin_password_here
```

### Шаг 3: Генерация безопасного SECRET_KEY

Для генерации безопасного SECRET_KEY выполните:

```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

Скопируйте полученный ключ в `config.env`.

## 🔒 Проверка перед коммитом

### Перед каждым коммитом проверяйте:

1. **Не добавляйте config.env в Git:**
   ```bash
   git status
   # Убедитесь, что config.env не в списке измененных файлов
   ```

2. **Проверьте .gitignore:**
   ```bash
   cat .gitignore | grep config.env
   # Должно вывести: config.env
   ```

3. **Проверьте историю коммитов:**
   ```bash
   git log --all --full-history -- config.env
   # Если файл был в истории, его нужно удалить
   ```

## 🛡️ Если секреты попали в Git

Если вы случайно закоммитили `config.env`:

### 1. Удалите файл из истории Git:

```bash
# Удалить файл из истории (осторожно!)
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch config.env" \
  --prune-empty --tag-name-filter cat -- --all
```

### 2. Добавьте в .gitignore (если еще не добавлен):

```bash
echo "config.env" >> .gitignore
```

### 3. Создайте новый коммит:

```bash
git add .gitignore
git commit -m "Remove config.env from repository"
git push --force --all
```

## 🔐 Безопасное хранение секретов

### Для production окружения:

1. **Используйте переменные окружения:**
   - Настройте секреты в переменных окружения сервера
   - Не храните секреты в файлах на сервере

2. **Используйте менеджеры секретов:**
   - AWS Secrets Manager
   - HashiCorp Vault
   - Azure Key Vault
   - Google Secret Manager

3. **Шифрование:**
   - Используйте шифрование для чувствительных данных в базе данных
   - Настройте TLS/SSL для всех соединений

## 📝 Чеклист перед деплоем

- [ ] `config.env` не в Git репозитории
- [ ] `.gitignore` содержит `config.env`
- [ ] Все секретные данные заполнены в `config.env`
- [ ] `SECRET_KEY` сгенерирован и не дефолтный
- [ ] `ADMIN_PASSWORD` изменен с дефолтного значения
- [ ] `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` заполнены
- [ ] SMTP настройки корректны
- [ ] `ENVIRONMENT=production` для production сервера
- [ ] Все порты не конфликтуют
- [ ] CORS настройки ограничены для production

## 🚨 В случае утечки секретов

1. **Немедленно смените все секреты:**
   - Telegram Bot Token
   - SMTP пароли
   - API ключи
   - JWT SECRET_KEY

2. **Проверьте логи на подозрительную активность**

3. **Уведомите команду о потенциальной утечке**

## 📞 Поддержка

При возникновении вопросов по безопасности обращайтесь к администратору проекта.

