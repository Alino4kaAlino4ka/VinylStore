## Практические уроки: Извлечение промптов и дополнительных материалов

### **Урок 1: Основы проекта и настройка среды**

#### 🤖 **Промпт для AI-ассистента:**
```
Привет! Ты мой AI-ассистент по разработке. Мы начинаем большой проект: интернет-магазин аудиокниг.

Задача: создай, пожалуйста, базовую структуру папок и файлов для этого проекта.
Наш технологический стек: фронтенд будет на чистом HTML, CSS и JavaScript. В будущем мы добавим бэкенд на Python с использованием фреймворка FastAPI.

Требования к структуре:
1.  Она должна быть логичной и легко расширяемой.
2.  Создай главную папку `src` для всех исходников фронтенда.
3.  Внутри `src` создай папки `styles` для CSS, `scripts` для JS и `assets/images` для изображений.
4.  Создай базовые файлы: `index.html` в корне `src`, `style.css` в `src/styles` и `script.js` в `src/scripts`.
5.  Важно: в `index.html` сразу подключи файлы `style.css` и `script.js` с правильными путями.
```

---

### **Урок 2: Архитектура проекта и Техническое Задание**

#### 🤖 **Промпт для AI-архитектора и бизнес-аналитика (составление ТЗ):**
```
Представь, что ты AI-архитектор и бизнес-аналитик в одном лице. Я получил следующий бриф от клиента для нашего проекта "audio-store":

"Мы хотим создать современный и удобный интернет-магазин аудиокниг. Главные функции, которые нам нужны:
- Большой каталог книг с возможностью фильтрации по жанрам, авторам и рейтингу.
- Страница для каждой книги с подробным описанием, отзывами и встроенным плеером для прослушивания фрагмента.
- Пользовательская корзина для добавления выбранных книг.
- Простая система регистрации и авторизации.
- Личный кабинет, где пользователь может видеть свою историю покупок и список приобретенных книг."

Твоя задача: на основе этого брифа составить краткое и структурированное Техническое Задание (ТЗ).

В ТЗ должны быть следующие разделы:
1.  **Главная цель проекта.**
2.  **Основные функциональные модули** (опиши каждый модуль 1-2 предложениями).
3.  **Предлагаемый технологический стек** (используй тот, что мы обсуждали: фронтенд на HTML/CSS/JS, а бэкенд на Python с фреймворком FastAPI).
```

#### 🤖 **Промпт для AI-архитектора (создание структуры папок):**
```
Основываясь на функциональных модулях, которые ты только что описал в ТЗ, создай структуру папок для микросервисной архитектуры в корне нашего проекта.

Требования:
1.  Создай в корне проекта общую папку с названием `services`.
2.  Внутри `services` для каждого ключевого домена (каталог, пользователи, заказы) создай отдельную папку. Названия должны быть короткими и на английском: `catalog`, `users`, `orders`.
3.  Внутри папки каждого микросервиса создай базовый файл `main.py` и добавь в него комментарий-заглушку, который описывает назначение этого сервиса. Например, для сервиса `users`: `# Этот сервис отвечает за регистрацию, авторизацию и управление профилями пользователей.`
```

---

### **Урок 3: От Figma к коду. Инструментарий AI-Верстальщика**

#### 🤖 **Промпт для Cursor AI (Header):**
```
Ты — frontend-разработчик. Проанализируй скриншот шапки сайта.

Задача:
1.  Создай семантический HTML-код для этой шапки, используя теги `<header>`, `<nav>`. Вставь его в текущий файл (`index.html`).
2.  Напиши все необходимые CSS-стили и добавь их в конец открытого файла `style.css`.
3.  Структура шапки слева направо:
    - Логотип в виде текста "Аудитерия".
    - Навигационное меню (`<nav>`) с тремя ссылками: "Мои книги", "Каталог", "Закладки". Пункт "Каталог" должен быть активным (добавь ему класс `active`).
    - Справа группа элементов: иконка поиска, имя пользователя "Иван Иванов" и кнопка "Войти". Для иконки пока используй текстовый плейсхолдер, например, символ лупы (🔍).

Сделай так, чтобы все элементы были выровнены по горизонтали и вертикали, как на макете.
```

#### 🤖 **Промпт для Cursor AI (Footer):**
```
Ты — frontend-разработчик. Вот скриншот футера сайта и точные стили из Figma.

Задача:
1.  Создай HTML-структуру для футера с тегом `<footer>`. Код вставь в `index.html`. CSS добавь в `style.css`.
2.  Общий фон футера: `#0F1014`.
3.  В левой части футера размести логотип (пока просто текстом "Аудитерия").
4.  В правой части создай три колонки с заголовками "Жанры", "Категории", "Разделы". Каждая колонка содержит список ссылок (можешь взять названия с картинки).
5.  Используй эти точные стили из Figma:
    - **Для заголовков колонок** ("Жанры" и т.д.): `color: #D9D9D9;`, `font-size: 21px;`, `font-weight: 700;`.
    - **Для ссылок в колонках**: `color: #ADADAD;`, `font-size: 12px;`, `font-weight: 300;`, `text-decoration: underline;`.

Расположи все элементы согласно скриншоту.
```

#### 🤖 **Промпт для Cursor AI (Карточки товаров):**
```
Вот ссылка на Figma-компонент "Audiobook Card": [вставить скопированную ссылку]

Задача:
1.  Создай секцию-контейнер с тегом `<main>` и классом `catalog-grid`.
2.  Проанализируй компонент по ссылке и создай для него HTML и CSS.
3.  Размести внутри `.catalog-grid` 4 экземпляра сгенерированного HTML-кода для этой карточки товара.
4.  Для контейнера `.catalog-grid` используй CSS Grid, чтобы карточки выстроились в сетку 2x2. Установи отступы между ячейками (gap) в 20px.
5.  Весь HTML вставь в `index.html`, а CSS — в `style.css`.
```

#### 📚 **Дополнительные материалы к Уроку 3:**
*   [Figma Dev Mode: Руководство](https://help.figma.com/hc/en-us/articles/32132100833559-Guide-to-the-Dev-Mode-MCP-Server)
*   [Figma Dev Mode: Обзор](https://help.figma.com/hc/en-us/articles/15023124644247-Guide-to-Dev-Mode)
*   [Figma для разработчиков: Режим Dev Mode](https://vc.ru/dev/835066-figma-dlya-razrabotchikov-rezhim-dev-mode)
*   [Как и чем заменить Dev Mode в Figma](https://dtf.ru/howto/2504859-instrukciya-kak-i-chem-zamenit-dev-mode-v-figma)
*   [Figma для VS Code](https://help.figma.com/hc/en-us/articles/15023121296151-Figma-for-VS-Code)

---

### **Урок 4: Реализация домена "Каталог" с помощью DDD и базы данных SQLite**

#### 🤖 **Промпт для Cursor AI (Полная модель данных):**
```
Ты — архитектор данных, мыслящий в терминах DDD.

Опиши полную доменную модель для домена "Каталог" с использованием SQLAlchemy.

Модель должна включать следующие сущности и связи между ними:

1.  **Сущность `Author` (Автор):**
    - Уникальный идентификатор.
    - Имя автора.

2.  **Сущность `Category` (Категория):**
    - Уникальный идентификатор.
    - Название категории.

3.  **Агрегат `Audiobook` (Аудиокнига), который является корнем:**
    - Уникальный идентификатор.
    - Название.
    - Описание.
    - Цена.
    - Ссылка на изображение обложки.

**Установи между ними связи:**
-   Между `Author` и `Audiobook` должна быть связь "один-ко-многим" (один автор может иметь много аудиокниг).
-   Между `Audiobook` и `Category` должна быть связь "многие-ко-многим" (аудиокнига может быть в нескольких категориях, и категория может содержать много аудиокниг).
```

#### 🤖 **Промпт для Cursor AI (Настройка подключения):**
```
Ты — системный инженер.

Создай модуль, отвечающий за подключение к нашей базе данных SQLite. Этот модуль будет служить единой точкой доступа к слою персистентности для всех микросервисов.

Требования к модулю:
1.  Он должен настраивать подключение к локальному файлу базы данных `audio_store.db`.
2.  Он должен предоставлять 'фабрику сессий' для создания сеансов работы с базой данных.
3.  Он должен содержать функцию для инициализации базы — создания всех таблиц на основе описанных нами доменных моделей.
```

#### 🤖 **Промпт для Cursor AI (Микросервис с вложенными данными):**
```
Создай приложение FastAPI, которое будет служить прикладным слоем (Application Layer) для нашего домена "Каталог".

Основное бизнес-требование — предоставить API-эндпоинт для получения списка всех аудиокниг с их полной, связанной информацией.

1.  **API-эндпоинт:** Реализуй эндпоинт `GET /api/v1/audiobooks`.
2.  **Ответ с богатыми данными:** Эндпоинт должен возвращать список аудиокниг. Для каждой аудиокниги в JSON-ответе должны содержаться вложенные данные о ее авторе и список ее категорий.
3.  **Объекты передачи данных (DTO):** Определи все необходимые схемы Pydantic для создания чистого публичного контракта данных. Скорее всего, потребуются вложенные схемы (например, `AuthorSchema` внутри `AudiobookSchema`).
4.  **Доступ к базе данных:** Используй стандартный паттерн FastAPI с внедрением зависимостей для управления сессиями базы данных. Убедись, что запрос к базе данных эффективно подгружает связанные данные `Author` и `Category`, чтобы избежать проблемы производительности N+1.
```

#### 🤖 **Промпт для Cursor AI (Скрипт для сидинга):**
```
Напиши Python-скрипт для наполнения нашей базы данных SQLAlchemy тестовыми данными.

Скрипт должен:
1.  Использовать наши модели `Author`, `Category`, `Audiobook` и фабрику сессий `SessionLocal`.
2.  Создать одного автора (например, "Льюис Кэрролл").
3.  Создать три категории ("Фэнтези", "Детская литература", "Классика").
4.  Создать две аудиокниги, связав их с созданным автором и категориями. Например:
    - "Алиса в Стране чудес" (связать с категориями "Фэнтези", "Детская литература").
    - "Алиса в Зазеркалье" (связать с категориями "Фэнтези", "Классика").
5.  Сохранить все созданные объекты в базу данных, используя одну сессию.
```

---

### **Урок 5: Домены "Корзина" и "Заказы". Межсервисное взаимодействие**

#### 🤖 **Промпт для Cursor AI (Микросервис корзины):**
```
Ты — backend-разработчик. Создай микросервис "Корзина" на FastAPI.

Основная бизнес-задача сервиса — валидация и расчет стоимости корзины.

1.  **API-эндпоинт:** Реализуй эндпоинт `POST /api/v1/cart/calculate`.
2.  **Входные и выходные данные:**
    -   На вход эндпоинт принимает DTO с сырым списком товаров (ID аудиокниги и количество).
    -   На выход он возвращает DTO с полным списком товаров (включая название, цену за единицу) и итоговой стоимостью всей корзины (`total_price`).
3.  **Межсервисное взаимодействие:**
    -   Для получения информации о каждой аудиокниге (включая ее цену), сервис должен асинхронно обратиться по API к микросервису "Каталог" по адресу `http://localhost:8000/api/v1/audiobooks/{audiobook_id}`. Используй для этого библиотеку `httpx`.
    -   Если товар в каталоге не найден (ошибка 404), он должен быть проигнорирован.
```

#### 🤖 **Промпт для Cursor AI (Микросервис заказов):**
```
Ты — backend-разработчик. Создай микросервис "Заказы" на FastAPI.

**Бизнес-логика:**
1.  Реализуй эндпоинт `POST /api/v1/orders` для создания нового заказа.
2.  На вход он принимает "сырой" состав корзины (список ID аудиокниг и их количество).
3.  **Ключевой шаг:** Перед созданием заказа, он должен сначала **обратиться к микросервису "Корзина"** по адресу `http://localhost:8001/api/v1/cart/calculate`, чтобы получить валидированный состав и итоговую стоимость.
4.  На основе ответа от сервиса "Корзина" он должен **транзакционно** создать запись в таблице `Orders` и все связанные записи в `OrderItems`, фиксируя цену на момент покупки.
5.  Вернуть информацию о созданном заказе.
```