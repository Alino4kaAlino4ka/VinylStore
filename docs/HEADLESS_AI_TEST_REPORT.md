# Отчет о тестировании Headless AI архитектуры (Шаг 2)

## ✅ Выполненные изменения

### 1. Рефакторинг recommender/main.py

- ✅ Добавлена функция `get_prompt_from_manager()` для получения промптов из prompts-manager
- ✅ Функция `create_system_prompt()` теперь асинхронная и получает базовый промпт из prompts-manager
- ✅ Эндпоинт `/api/v1/recommendations/generate` использует промпты из prompts-manager
- ✅ Эндпоинт `/api/v1/recommendations/generate-description/{product_id}` использует промпты из prompts-manager

### 2. Исправления

- ✅ Исправлен порт prompts-manager с 8006 на 8007 (соответствует реальной конфигурации проекта)

## 🧪 Тестирование

### Созданные тесты

1. **test_headless_ai.py** - Полный комплексный тест
   - Проверка доступности сервисов
   - Проверка получения промптов из prompts-manager
   - Проверка использования промптов в recommender

2. **test_headless_ai_simple.py** - Простой тест
   - Быстрая проверка получения промптов

### Запуск тестов

```bash
# Полный тест (требует запущенные сервисы)
python test_headless_ai.py

# Простой тест получения промптов
python test_headless_ai_simple.py
```

## 📋 Необходимые сервисы

Для работы Headless AI архитектуры должны быть запущены:

1. **prompts-manager** (порт 8007) - управление промптами
2. **recommender** (порт 8004) - генерация рекомендаций
3. **catalog** (порт 8000) - каталог пластинок (нужен для рекомендаций)

### Запуск сервисов

```bash
# Автоматический запуск всех сервисов
python start_services_final.py

# Или отдельно:
start_prompts_manager.bat
```

## 🔍 Проверка работы

### 1. Проверка prompts-manager

```bash
# Health check
curl http://localhost:8007/health

# Получение промпта
curl http://localhost:8007/api/v1/prompts/recommendation_prompt
curl http://localhost:8007/api/v1/prompts/description_prompt
```

### 2. Проверка recommender

```bash
# Health check
curl http://localhost:8004/health

# Генерация рекомендаций (должен использовать промпт из prompts-manager)
curl -X POST http://localhost:8004/api/v1/recommendations/generate \
  -H "Content-Type: application/json" \
  -d '{"user_preferences": "фантастика", "max_recommendations": 3}'
```

## ✅ Ожидаемое поведение

1. **При старте prompts-manager:**
   - Автоматически создаются дефолтные промпты:
     - `recommendation_prompt` - для генерации рекомендаций
     - `description_prompt` - для генерации описаний

2. **При запросе рекомендаций:**
   - Recommender делает GET запрос к `http://localhost:8007/api/v1/prompts/recommendation_prompt`
   - Получает поле `content` из ответа
   - Использует этот промпт как базовый, дополняя его динамическими данными (каталог, предпочтения)

3. **При генерации описания:**
   - Recommender делает GET запрос к `http://localhost:8007/api/v1/prompts/description_prompt`
   - Получает поле `content` из ответа
   - Использует этот промпт как базовый, дополняя его информацией о пластинке

## 🐛 Отладка

### Проблема: prompts-manager недоступен (503)

**Решение:** Убедитесь, что prompts-manager запущен на порту 8007:
```bash
start_prompts_manager.bat
# или
python start_services_final.py
```

### Проблема: Промпт не найден (404)

**Решение:** Промпты должны создаваться автоматически при старте prompts-manager. Проверьте:
```bash
curl http://localhost:8007/api/v1/prompts
```

Если промптов нет, перезапустите prompts-manager.

### Проблема: Recommender не получает промпты

**Решение:** 
1. Проверьте логи recommender - должны быть сообщения о получении промптов
2. Убедитесь, что prompts-manager доступен с того же хоста
3. Проверьте, что порт 8007 не занят другим процессом

## 📝 Логи

При работе recommender должен логировать:
```
[Шаг 2] Получаем базовый промпт из prompts-manager...
[Шаг 4] Создаем сложный системный промпт (Headless AI - получаем из prompts-manager)
```

## ✅ Результаты тестирования

- ✅ Функция получения промптов работает корректно
- ✅ Обработка ошибок реализована (404, 503, таймауты)
- ✅ Эндпоинты используют промпты из prompts-manager
- ✅ Динамические данные корректно дополняют базовые промпты

## 🎯 Следующие шаги

После успешного тестирования шага 2 можно переходить к шагу 3:
- Создание админ-панели для управления промптами (CRUD операции)
- Редактор промптов с версионированием

