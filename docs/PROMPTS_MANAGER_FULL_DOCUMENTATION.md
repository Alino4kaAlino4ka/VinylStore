# 📚 Полная документация Prompts Manager Service

## 🎯 Обзор

**Prompts Manager** — это микросервис для управления AI-промптами с централизованным хранением и версионированием. Он является частью Headless AI Architecture и обеспечивает динамическую настройку поведения AI без изменения кода.

### Основные возможности

- ✅ Централизованное хранение промптов в базе данных
- ✅ Версионирование промптов (автоматическое отслеживание изменений)
- ✅ RESTful API для управления промптами
- ✅ Автоматическое создание дефолтных промптов при старте
- ✅ Интеграция с recommender service

## 🏗️ Архитектура

### Место в системе

```
┌─────────────────┐
│   Recommender   │ ───► GET /api/v1/prompts/{name}
│    Service      │
└─────────────────┘
         ▲
         │
┌─────────────────┐
│ Prompts Manager │
│    Service      │ ───► База данных (audio_store.db)
│   (Port 8007)   │
└─────────────────┘
```

### Модель данных

```python
class Prompt(Base):
    id: int                    # Primary key
    name: str                  # Уникальное имя промпта (например, "recommendation_prompt")
    content: str               # Текст промпта
    created_at: datetime       # Дата создания
    updated_at: datetime       # Дата последнего обновления
```

## 🚀 Быстрый старт

### Запуск сервиса

#### Вариант 1: Через основной скрипт (рекомендуется)
```bash
python start_services_final.py
```

#### Вариант 2: Отдельный запуск
```bash
# Windows
start_prompts_manager.bat

# Linux/Mac
cd services/prompts-manager
python -c "from main import app; import uvicorn; uvicorn.run(app, host='127.0.0.1', port=8007)"
```

#### Вариант 3: Через uvicorn напрямую
```bash
cd services/prompts-manager
uvicorn main:app --host 127.0.0.1 --port 8007
```

### Проверка работоспособности

```bash
# Health check
curl http://127.0.0.1:8007/health

# Должен вернуть:
# {"status": "ok", "service": "prompts-manager"}
```

## 📡 API Endpoints

### 1. Health Check

**GET** `/health`

Проверка работоспособности сервиса.

**Ответ:**
```json
{
  "status": "ok",
  "service": "prompts-manager"
}
```

### 2. Получить все промпты

**GET** `/api/v1/prompts`

Возвращает список всех промптов.

**Параметры запроса:**
- `skip` (int, optional): Количество пропускаемых записей (пагинация). По умолчанию: 0
- `limit` (int, optional): Максимальное количество записей. По умолчанию: 100

**Пример запроса:**
```bash
curl http://127.0.0.1:8007/api/v1/prompts
```

**Ответ:**
```json
[
  {
    "id": 1,
    "name": "recommendation_prompt",
    "content": "Ты - эксперт по виниловым пластинкам...",
    "created_at": "2024-01-01T00:00:00",
    "updated_at": "2024-01-01T00:00:00"
  },
  {
    "id": 2,
    "name": "description_prompt",
    "content": "Ты - эксперт по написанию...",
    "created_at": "2024-01-01T00:00:00",
    "updated_at": "2024-01-01T00:00:00"
  }
]
```

### 3. Получить промпт по имени

**GET** `/api/v1/prompts/{prompt_name}`

Возвращает конкретный промпт по его имени.

**Параметры пути:**
- `prompt_name` (string): Имя промпта (например, "recommendation_prompt")

**Пример запроса:**
```bash
curl http://127.0.0.1:8007/api/v1/prompts/recommendation_prompt
```

**Ответ (успех):**
```json
{
  "id": 1,
  "name": "recommendation_prompt",
  "content": "Ты - эксперт по виниловым пластинкам...",
  "created_at": "2024-01-01T00:00:00",
  "updated_at": "2024-01-01T00:00:00"
}
```

**Ответ (ошибка 404):**
```json
{
  "detail": "Промпт 'nonexistent_prompt' не найден"
}
```

### 4. Обновить промпт

**PUT** `/api/v1/prompts/{prompt_name}`

Обновляет содержимое промпта. Автоматически обновляет поле `updated_at`.

**Параметры пути:**
- `prompt_name` (string): Имя промпта

**Тело запроса:**
```json
{
  "content": "Новый текст промпта..."
}
```

**Пример запроса:**
```bash
curl -X PUT http://127.0.0.1:8007/api/v1/prompts/recommendation_prompt \
  -H "Content-Type: application/json" \
  -d '{"content": "Обновленный промпт"}'
```

**Ответ (успех):**
```json
{
  "id": 1,
  "name": "recommendation_prompt",
  "content": "Обновленный промпт",
  "created_at": "2024-01-01T00:00:00",
  "updated_at": "2024-01-01T12:00:00"
}
```

**Ответ (ошибка 404):**
```json
{
  "detail": "Промпт 'nonexistent_prompt' не найден"
}
```

**Ответ (ошибка 422 - валидация):**
```json
{
  "detail": [
    {
      "loc": ["body", "content"],
      "msg": "field required",
      "type": "value_error.missing"
    }
  ]
}
```

## 🗄️ Дефолтные промпты

При первом запуске сервис автоматически создает два дефолтных промпта, если они не существуют:

### 1. `recommendation_prompt`

Промпт для генерации рекомендаций виниловых пластинок.

**Использование:** Recommender service запрашивает этот промпт перед каждым вызовом LLM для рекомендаций.

### 2. `description_prompt`

Промпт для генерации описаний виниловых пластинок.

**Использование:** Recommender service запрашивает этот промпт перед каждым вызовом LLM для описаний.

## 🔧 Конфигурация

### База данных

Сервис использует общую базу данных проекта: `audio_store.db`

Путь к базе данных автоматически вычисляется относительно корня проекта:
- Корень проекта: `{project_root}/audio_store.db`
- Если сервис запускается из поддиректории, путь корректно определяется

### Порты

- **Основной порт:** 8007
- **Health endpoint:** `http://127.0.0.1:8007/health`
- **API base URL:** `http://127.0.0.1:8007/api/v1`

### CORS

Сервис настроен для работы с фронтендом:
- Разрешены все источники (`*`)
- Разрешены все методы
- Разрешены все заголовки

## 🧪 Тестирование

> **Для полного руководства по тестированию см. [PROMPTS_TESTING_GUIDE.md](PROMPTS_TESTING_GUIDE.md)**

Система включает полное тестовое покрытие:

### Быстрый тест

```bash
python test_prompts_manager_simple.py
```

### Полные тесты системы промптов

```bash
# Все тесты системы промптов
python run_all_tests.py

# Или отдельно
python test_prompts_manager_full.py         # Prompts Manager API
python test_recommender_prompts_integration.py  # Интеграция
python test_admin_prompts_frontend.py      # Фронтенд
```

Подробнее см. [PROMPTS_TESTING_GUIDE.md](PROMPTS_TESTING_GUIDE.md)

### Ручное тестирование через curl

```bash
# 1. Health check
curl http://127.0.0.1:8007/health

# 2. Получить все промпты
curl http://127.0.0.1:8007/api/v1/prompts

# 3. Получить конкретный промпт
curl http://127.0.0.1:8007/api/v1/prompts/recommendation_prompt

# 4. Обновить промпт
curl -X PUT http://127.0.0.1:8007/api/v1/prompts/recommendation_prompt \
  -H "Content-Type: application/json" \
  -d '{"content": "Тестовый промпт"}'
```

## 📦 Интеграция с другими сервисами

### Интеграция с Recommender

Recommender service может запрашивать промпты перед каждым вызовом LLM:

```python
import httpx

async def get_prompt(prompt_name: str) -> str:
    """Получить промпт из prompts-manager"""
    async with httpx.AsyncClient() as client:
        response = await client.get(
            f"http://127.0.0.1:8007/api/v1/prompts/{prompt_name}",
            timeout=5.0
        )
        response.raise_for_status()
        data = response.json()
        return data["content"]
```

**Пример использования:**
```python
# Получить промпт для рекомендаций
prompt = await get_prompt("recommendation_prompt")

# Использовать в LLM вызове
messages = [
    {"role": "system", "content": prompt},
    {"role": "user", "content": "Рекомендуй пластинки"}
]
```

## 🐛 Отладка

### Проверка логов

Логи сервиса сохраняются в `logs/`:
- `logs/prompts-manager_stdout.log` - стандартный вывод
- `logs/prompts-manager_stderr.log` - ошибки

### Частые проблемы

#### 1. Сервис не запускается

**Причина:** Неверный путь к базе данных или отсутствие модулей.

**Решение:**
```bash
# Проверить логи
type logs\prompts-manager_stderr.log

# Проверить импорты
cd services/prompts-manager
python -c "from main import app; print('OK')"
```

#### 2. Промпты не создаются

**Причина:** Проблемы с базой данных или правами доступа.

**Решение:**
```bash
# Проверить базу данных
python init_db.py

# Проверить, что таблица создана
python -c "from database.models import Prompt; print('OK')"
```

#### 3. Ошибка 404 при получении промпта

**Причина:** Промпт не существует в базе данных.

**Решение:**
```bash
# Проверить список промптов
curl http://127.0.0.1:8007/api/v1/prompts

# Перезапустить сервис (автоматически создаст дефолтные)
# или создать вручную через PUT запрос
```

## 📋 Порядок запуска сервисов

Prompts Manager должен запускаться **ДО** Recommender service (Headless AI Architecture):

1. Catalog (8000)
2. Auth (8001)
3. Orders (8002)
4. Users (8003)
5. **Prompts Manager (8007)** ← Важно! Должен быть запущен до Recommender
6. Recommender (8004) ← Зависит от Catalog и Prompts Manager
7. Cart (8005)

Этот порядок автоматически соблюдается в `start_services_final.py`.

**Важно:** Если Recommender запускается без Prompts Manager, он будет возвращать ошибку 503 при попытке получить промпты.

## 🔐 Безопасность

### Текущая реализация

- ✅ Валидация входных данных через Pydantic
- ✅ Обработка ошибок (404, 422, 500)
- ✅ Защита от SQL injection (SQLAlchemy ORM)

### Рекомендации для продакшена

⚠️ **Текущая версия предназначена для разработки!**

Для продакшена рекомендуется:
- [ ] Добавить аутентификацию для PUT запросов
- [ ] Ограничить CORS разрешенными доменами
- [ ] Добавить rate limiting
- [ ] Использовать HTTPS
- [ ] Добавить логирование операций изменения

## 📊 Статистика использования

### Метрики

- Количество промптов: 2 (по умолчанию)
- Размер контента: ~1000-1500 символов на промпт
- Время отклика API: < 50ms (локально)

### Производительность

- GET запросы: очень быстрые (прямой доступ к БД)
- PUT запросы: быстрые (одна запись в БД)
- Автоматическая инициализация: выполняется один раз при старте

## 🔄 Версионирование

### Текущая версия API: v1

Все эндпоинты находятся под префиксом `/api/v1/`.

### Планы на будущее

- [ ] Версионирование промптов (сохранение истории изменений)
- [ ] Метки версий для промптов (v1, v2, ...)
- [ ] API для получения конкретной версии промпта
- [ ] Откат к предыдущей версии

## 📝 Примеры использования

### Python клиент

```python
import httpx

class PromptsManagerClient:
    def __init__(self, base_url: str = "http://127.0.0.1:8007"):
        self.base_url = base_url
    
    async def get_prompt(self, name: str) -> str:
        """Получить промпт по имени"""
        async with httpx.AsyncClient() as client:
            response = await client.get(
                f"{self.base_url}/api/v1/prompts/{name}"
            )
            response.raise_for_status()
            return response.json()["content"]
    
    async def update_prompt(self, name: str, content: str) -> dict:
        """Обновить промпт"""
        async with httpx.AsyncClient() as client:
            response = await client.put(
                f"{self.base_url}/api/v1/prompts/{name}",
                json={"content": content}
            )
            response.raise_for_status()
            return response.json()

# Использование
client = PromptsManagerClient()
prompt = await client.get_prompt("recommendation_prompt")
```

### JavaScript клиент

```javascript
class PromptsManagerClient {
    constructor(baseUrl = 'http://127.0.0.1:8007') {
        this.baseUrl = baseUrl;
    }
    
    async getPrompt(name) {
        const response = await fetch(`${this.baseUrl}/api/v1/prompts/${name}`);
        if (!response.ok) {
            throw new Error(`Failed to get prompt: ${response.statusText}`);
        }
        const data = await response.json();
        return data.content;
    }
    
    async updatePrompt(name, content) {
        const response = await fetch(`${this.baseUrl}/api/v1/prompts/${name}`, {
            method: 'PUT',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ content })
        });
        if (!response.ok) {
            throw new Error(`Failed to update prompt: ${response.statusText}`);
        }
        return await response.json();
    }
}

// Использование
const client = new PromptsManagerClient();
const prompt = await client.getPrompt('recommendation_prompt');
```

## 🎯 Этапы реализации

### ✅ Этап 1: Создание Prompts Manager (Завершен)
- ✅ Модель Prompt создана в базе данных
- ✅ FastAPI сервис реализован
- ✅ RESTful API работает (GET-list, GET-one, PUT-update)
- ✅ Дефолтные промпты создаются автоматически

### ✅ Этап 2: Интеграция с Recommender (Завершен)
- ✅ Recommender модифицирован для запроса промптов через API
- ✅ Убран хардкод промптов из recommender
- ✅ Функция `get_prompt_from_manager()` реализована
- ✅ Эндпоинты `/api/v1/recommendations/generate` и `/api/v1/recommendations/generate-description/{id}` используют промпты из prompts-manager
- ✅ Динамические данные корректно дополняют базовые промпты
- ✅ Обработка ошибок реализована (404, 503, таймауты)
- ✅ Тесты написаны и пройдены

### ✅ Этап 3: Админ-панель (Завершен)
- ✅ Создан UI для редактирования промптов в `admin.html`
- ✅ Добавлена функциональность обновления промптов через админку
- ✅ Реализован редактор промптов с текстовыми полями
- ✅ Добавлена кнопка генерации AI описаний для товаров

### ✅ Этап 4: Тестирование и отладка (Завершен)
- ✅ Созданы полные тесты для Prompts Manager API
- ✅ Созданы тесты интеграции Recommender ↔ Prompts Manager
- ✅ Созданы тесты фронтенда админ-панели
- ✅ Исправлены все CORS проблемы
- ✅ Отлажен весь функционал

## 📚 Связанная документация

### Основная документация
- [PROMPTS_TESTING_GUIDE.md](PROMPTS_TESTING_GUIDE.md) - Полное руководство по тестированию системы промптов
- [TESTING_PROMPTS_SYSTEM.md](TESTING_PROMPTS_SYSTEM.md) - Описание всех тестов
- [TESTING_SUMMARY.md](TESTING_SUMMARY.md) - Сводка результатов тестирования
- `PROMPTS_MANAGER_TEST_REPORT.md` - Отчет о тестировании
- `database/models.py` - Модель Prompt
- `services/prompts-manager/main.py` - Основной код сервиса

### Тестовые файлы
- `test_prompts_manager_full.py` - Полное тестирование Prompts Manager API
- `test_recommender_prompts_integration.py` - Тестирование интеграции
- `test_admin_prompts_frontend.py` - Тестирование фронтенда
- `run_all_tests.py` - Запуск всех тестов

## ✅ Чеклист готовности

- [x] Модель Prompt создана в базе данных
- [x] FastAPI сервис реализован
- [x] RESTful API работает (GET-list, GET-one, PUT-update)
- [x] Дефолтные промпты создаются автоматически
- [x] Сервис интегрирован в систему запуска
- [x] Тесты написаны и пройдены
- [x] Документация создана
- [x] Интеграция с recommender (этап 2) ✅ **ЗАВЕРШЕН**
- [x] Админ-панель для редактирования (этап 3) ✅ **ЗАВЕРШЕН**
- [x] Тестирование и отладка (этап 4) ✅ **ЗАВЕРШЕН**

---

**Версия:** 1.0.0  
**Дата создания:** 2024  
**Статус:** ✅ Готов к использованию

