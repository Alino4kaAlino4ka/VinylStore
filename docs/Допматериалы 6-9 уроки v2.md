## Практические уроки: Промпты и дополнительные материалы (Уроки 6-9, v2)

### **Урок 6: Домен "Аутентификация" и Единый Язык (Ubiquitous Language)**

*(Этот раздел без изменений, т.к. урок не затрагивался рекомендациями)*

#### 🤖 **Промпт для AI-ассистента (Backend: Модели и безопасность):**
```
... (существующий промпт)
```
#### 🤖 **Промпт для AI-ассистента (Frontend: Формы регистрации и входа):**
```
... (существующий промпт)
```
#### 🤖 **Промпт для AI-ассистента (Интеграция с UI):**
```
... (существующий промпт)
```

---

### **Урок 7: Админ-панель и UI-клиенты для Ограниченных Контекстов**

#### 🤖 **Промпт для AI-ассистента (Backend: CRUD для Каталога):**
```
Задача: Дополни этот микросервис полным набором CRUD-операций для управления товарами (`Audiobook`).

**Требования:**
1.  Создай Pydantic-схемы `AudiobookCreate` и `AudiobookUpdate` для данных, необходимых для создания и обновления товара.
2.  **Реализуй или убедись в наличии следующих эндпоинтов:**
    -   `POST /api/v1/audiobooks`: Создает новый товар.
    -   `PUT /api/v1/audiobooks/{book_id}`: Обновляет товар по его ID.
    -   `DELETE /api/v1/audiobooks/{book_id}`: Удаляет товар по его ID.
    -   `GET /api/v1/audiobooks/{book_id}`: Получает один товар по ID.
3.  Не забудь для каждого эндпоинта использовать зависимость `get_db` и обрабатывать случай, когда товар с указанным ID не найден (возвращать ошибку 404).
```

#### 🤖 **Промпт для AI-ассистента (Frontend: Админ-панель с редактированием):**
```
Задача: Создай HTML-страницу с JavaScript для администрирования каталога товаров, включая редактирование.

**`admin.html`:**
1.  Заголовок: "Панель администратора".
2.  Секция "Добавить/Редактировать товар":
    -   Форма с `id="product-form"`.
    -   Поля input для: `title`, `author_id`, `description`, `price`.
    -   Кнопка "Сохранить".
3.  Секция "Товары":
    -   Пустая таблица с `id="products-table"` для отображения товаров. В каждой строке - кнопки "Редактировать" и "Удалить".

**`admin.js`:**
1.  Напиши асинхронную функцию `fetchAndRenderProducts()` для получения и отрисовки списка товаров в таблице.
2.  При загрузке страницы вызови `fetchAndRenderProducts()`.
3.  Добавь обработчик `submit` на форму `product-form`. Он должен проверять, редактируется ли товар (например, по наличию `data-editing-id` у формы). Если да — отправлять `PUT` запрос, если нет — `POST` запрос. После любой операции — вызывать `fetchAndRenderProducts()`.
4.  Через делегирование событий повесь обработчик на `products-table`:
    -   При клике на "Удалить" — отправлять `DELETE` запрос.
    -   При клике на "Редактировать" — получать данные товара, заполнять ими форму и устанавливать `data-editing-id`.
```

#### 🤖 **Промпт для AI-ассистента (Frontend: Добавление в корзину на главной):**
```
Задача: Добавь на главную страницу функционал добавления в корзину.

1.  **В `index.html`**:
    -   Для каждой карточки товара добавь кнопку `<button class="add-to-cart-btn">В корзину</button>`.
    -   В шапку сайта добавь ссылку `<a href="/cart.html">Корзина (<span id="cart-count">0</span>)</a>`.

2.  **В `script.js`**:
    -   Напиши функции `getCart()`, `saveCart(cart)`, `updateCartCount()`. Корзина должна храниться в `localStorage`.
    -   Используя делегирование событий, повесь обработчик на клики по кнопкам `.add-to-cart-btn`, который добавляет ID товара в корзину и обновляет счетчик.
    -   При загрузке страницы вызывай `updateCartCount()`.
```

#### 🤖 **Промпт для AI-ассистента (Frontend: Страница корзины и заказ):**
```
Задача: Создай страницу корзины и логику оформления заказа.

1.  **Создай `src/cart.html`**:
    -   Базовая разметка с заголовком "Ваша корзина".
    -   Пустой контейнер `<div id="cart-items"></div>` для товаров.
    -   Итоговая сумма `<h3>Итого: <span id="total-price">0</span> ₽</h3>`.
    -   Кнопка `<button id="checkout-btn">Оформить заказ</button>`.

2.  **Создай `src/scripts/cart.js`**:
    -   При загрузке страницы:
        -   Прочитай ID товаров из `localStorage`.
        -   Отправь их на `POST /api/v1/cart/calculate` (сервис `cart` на порту 8001).
        -   Отрисуй полученный список товаров и итоговую стоимость.
    -   Повесь обработчик на кнопку `checkout-btn`:
        -   При клике он должен снова прочитать `localStorage` и отправить ID товаров на `POST /api/v1/orders` (сервис `orders` на порту 8002).
        -   После успешного заказа очисти корзину в `localStorage` и покажи `alert('Заказ успешно оформлен!')`.
```

---

### **Урок 8: AI-рекомендации и Ядро Домена (Core Domain)**

*(Этот раздел без изменений, т.к. урок не затрагивался рекомендациями)*
```
... (существующие промпты)
```

---

### **Урок 9: AI-генерация контента. Паттерны "Оркестратор" и "ACL"**

*(Этот раздел без изменений, т.к. урок не затрагивался рекомендациями)*
```
... (существующие промпты)
```
