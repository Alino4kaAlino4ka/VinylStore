# 🔧 Быстрое исправление: prompts-manager не запущен

## ❌ Проблема
```
ERR_CONNECTION_REFUSED
Промпты не найдены
```

## ✅ Решение

### Шаг 1: Запустите сервис prompts-manager

**Вариант A: Через основной скрипт (рекомендуется)**
```bash
python start_services_final.py
```

**Вариант B: Отдельный запуск**
```bash
start_prompts_manager.bat
```

**Вариант C: Вручную**
```bash
cd services/prompts-manager
python -c "from main import app; import uvicorn; uvicorn.run(app, host='127.0.0.1', port=8007, reload=True)"
```

### Шаг 2: Проверьте, что сервис запустился

Вы должны увидеть в консоли:
```
✅ База данных инициализирована успешно
✅ Создан дефолтный промпт: recommendation_prompt
✅ Создан дефолтный промпт: description_prompt
```

### Шаг 3: Проверьте доступность

Откройте в браузере или выполните в терминале:
```bash
curl http://127.0.0.1:8007/health
```

Должен вернуться:
```json
{"status": "ok", "service": "prompts-manager"}
```

### Шаг 4: Обновите страницу админ-панели

После запуска сервиса:
1. Обновите страницу админ-панели (F5)
2. Или нажмите кнопку "🔄 Попробовать снова" в разделе промптов

## 🎯 Что исправлено в коде

1. ✅ Улучшена обработка ошибок - теперь показывается понятное сообщение
2. ✅ Добавлена инструкция по запуску прямо в интерфейсе
3. ✅ Кнопка "Попробовать снова" для повторной попытки загрузки

## ⚠️ Важно

- Сервис должен быть запущен **до** открытия админ-панели
- Если используете `start_services_final.py`, все сервисы запустятся автоматически
- Порт 8007 должен быть свободен

