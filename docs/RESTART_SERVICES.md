# 🔄 Инструкция по перезапуску сервисов

## Проблема
После изменения кода сервисов необходимо перезапустить их, чтобы изменения вступили в силу.

## Быстрый перезапуск

### Шаг 1: Остановите текущие сервисы
Если сервисы запущены через `start_services_final.py`:
- Нажмите `Ctrl+C` в терминале, где запущены сервисы

Или проверьте и остановите процессы вручную:
```bash
# Windows
taskkill /F /IM python.exe

# Linux/Mac
pkill -f "uvicorn.*recommender"
```

### Шаг 2: Запустите сервисы заново
```bash
python start_services_final.py
```

Дождитесь сообщения:
```
✅ Все сервисы готовы к работе!
```

### Шаг 3: Проверьте статус
В другом терминале выполните:
```bash
python check_services_status.py
```

Все сервисы должны показать `✓ AVAILABLE`.

## Что изменилось

После перезапуска сервис рекомендаций будет:
- ✅ Использовать модель `gpt-4o-mini` вместо недоступной `gemini-flash-1.5-8b`
- ✅ Автоматически переключаться на `gemini-pro-1.5` при необходимости
- ✅ Показывать более понятные сообщения об ошибках

## Проверка работы

После перезапуска попробуйте:
1. Откройте админ-панель в браузере
2. Нажмите кнопку "AI-описание" для любого продукта
3. Проверьте логи: `logs/recommender_stdout.log`

Вы должны увидеть в логах:
```
[Шаг 3] Пробуем модель gpt-4o-mini (openai/gpt-4o-mini)...
[Шаг 3] Модель gpt-4o-mini успешно ответила!
```

## Альтернативные способы запуска

### Запуск только сервиса рекомендаций
```bash
# Windows
start_recommender.bat

# Или через Python
python -m uvicorn services.recommender.main:app --host 127.0.0.1 --port 8004 --reload
```

### Запуск через bat файл
```bash
start_services_fixed.bat
```

## Устранение проблем

### Проблема: Порт занят
```bash
# Проверьте, какой процесс занимает порт
netstat -ano | findstr :8004

# Остановите процесс по PID
taskkill /F /PID <PID>
```

### Проблема: Сервис не перезагружается
- Убедитесь, что используете флаг `--reload` (автоматическая перезагрузка при изменении файлов)
- Или полностью остановите и запустите заново

### Проблема: Изменения не применяются
1. Убедитесь, что файл `services/recommender/main.py` сохранен
2. Проверьте, что перезапустили именно нужный сервис
3. Проверьте логи на наличие ошибок запуска

