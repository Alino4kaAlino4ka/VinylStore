# CRUD API Documentation

## Микросервис Каталога (Catalog Service)

### Обзор

Микросервис каталога предоставляет полный набор CRUD операций для управления виниловыми пластинками, исполнителями и категориями. Сервис построен на FastAPI и использует SQLite базу данных с SQLAlchemy ORM.

**Базовый URL:** `http://127.0.0.1:8000`

### Запуск сервиса

```bash
# Через .bat файл
start_catalog.bat

# Или напрямую
cd services/catalog
python -c "from main import app; import uvicorn; uvicorn.run(app, host='127.0.0.1', port=8000, reload=True)"
```

---

## 🏥 Health Check

### GET /health

Проверка состояния сервиса.

**Ответ:**
```json
{
  "status": "ok"
}
```

---

## 📚 Виниловые пластинки (Products)

### Создание пластинки

**POST** `/api/v1/products`

**Тело запроса:**
```json
{
  "name": "Название пластинки",
  "description": "Описание пластинки (опционально)",
  "price": 19.99,
  "author_id": 1,
  "category_ids": [1, 2]
}
```

**Ответ (201 Created):**
```json
{
  "id": 1,
  "name": "Название пластинки",
  "description": "Описание пластинки",
  "price": 19.99,
  "cover_url": null,
  "author": {
    "id": 1,
    "name": "Имя исполнителя"
  },
  "categories": [
    {
      "id": 1,
      "name": "Название категории"
    }
  ]
}
```

**Ошибки:**
- `404` - Исполнитель не найден
- `404` - Одна или несколько категорий не найдены

### Получение пластинки по ID

**GET** `/api/v1/products/{product_id}`

**Ответ (200 OK):**
```json
{
  "id": 1,
  "name": "Название пластинки",
  "description": "Описание пластинки",
  "price": 19.99,
  "cover_url": null,
  "author": {
    "id": 1,
    "name": "Имя исполнителя"
  },
  "categories": [
    {
      "id": 1,
      "name": "Название категории"
    }
  ]
}
```

**Ошибки:**
- `404` - Пластинка не найдена

### Получение всех пластинок

**GET** `/api/v1/products`

**Параметры запроса:**
- `skip` (int, опционально) - Количество записей для пропуска (по умолчанию: 0)
- `limit` (int, опционально) - Максимальное количество записей (по умолчанию: 100)

**Пример:** `GET /api/v1/products?skip=0&limit=10`

**Ответ (200 OK):**
```json
[
  {
    "id": 1,
    "name": "Название пластинки",
    "description": "Описание пластинки",
    "price": 19.99,
    "cover_url": null,
    "author": {
      "id": 1,
      "name": "Имя исполнителя"
    },
    "categories": [
      {
        "id": 1,
        "name": "Название категории"
      }
    ]
  }
]
```

### Обновление пластинки

**PUT** `/api/v1/products/{product_id}`

**Тело запроса (все поля опциональны):**
```json
{
  "name": "Новое название",
  "description": "Новое описание",
  "price": 25.99,
  "author_id": 2,
  "category_ids": [1, 3]
}
```

**Ответ (200 OK):**
```json
{
  "id": 1,
  "name": "Новое название",
  "description": "Новое описание",
  "price": 25.99,
  "cover_url": null,
  "author": {
    "id": 2,
    "name": "Новый исполнитель"
  },
  "categories": [
    {
      "id": 1,
      "name": "Категория 1"
    },
    {
      "id": 3,
      "name": "Категория 3"
    }
  ]
}
```

**Ошибки:**
- `404` - Пластинка не найдена
- `404` - Исполнитель не найден
- `404` - Одна или несколько категорий не найдены

### Удаление пластинки

**DELETE** `/api/v1/products/{product_id}`

**Ответ (200 OK):**
```json
{
  "message": "Product deleted successfully"
}
```

**Ошибки:**
- `404` - Пластинка не найдена

---

## 👨‍💼 Исполнители (Artists)

### Создание исполнителя

**POST** `/api/v1/authors`

**Тело запроса:**
```json
{
  "name": "Имя исполнителя"
}
```

**Ответ (201 Created):**
```json
{
  "id": 1,
  "name": "Имя исполнителя"
}
```

### Получение всех исполнителей

**GET** `/api/v1/authors`

**Ответ (200 OK):**
```json
[
  {
    "id": 1,
    "name": "Имя исполнителя"
  },
  {
    "id": 2,
    "name": "Другой исполнитель"
  }
]
```

---

## 📂 Категории (Categories)

### Создание категории

**POST** `/api/v1/categories`

**Тело запроса:**
```json
{
  "name": "Название категории"
}
```

**Ответ (201 Created):**
```json
{
  "id": 1,
  "name": "Название категории"
}
```

### Получение всех категорий

**GET** `/api/v1/categories`

**Ответ (200 OK):**
```json
[
  {
    "id": 1,
    "name": "Фантастика"
  },
  {
    "id": 2,
    "name": "Детектив"
  }
]
```

---

## 🔧 Примеры использования

### Создание полного каталога

```bash
# 1. Создаем исполнителя
curl -X POST "http://127.0.0.1:8000/api/v1/authors" \
  -H "Content-Type: application/json" \
  -d '{"name": "The Beatles"}'

# 2. Создаем категории
curl -X POST "http://127.0.0.1:8000/api/v1/categories" \
  -H "Content-Type: application/json" \
  -d '{"name": "Рок"}'

curl -X POST "http://127.0.0.1:8000/api/v1/categories" \
  -H "Content-Type: application/json" \
  -d '{"name": "Поп-рок"}'

# 3. Создаем пластинку
curl -X POST "http://127.0.0.1:8000/api/v1/products" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Abbey Road",
    "description": "Легендарный альбом The Beatles",
    "price": 29.99,
    "author_id": 1,
    "category_ids": [1, 2]
  }'
```

### Обновление пластинки

```bash
curl -X PUT "http://127.0.0.1:8000/api/v1/products/1" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Abbey Road (Remastered)",
    "price": 34.99
  }'
```

### Получение информации

```bash
# Получить все пластинки
curl "http://127.0.0.1:8000/api/v1/products"

# Получить конкретную пластинку
curl "http://127.0.0.1:8000/api/v1/products/1"

# Получить всех исполнителей
curl "http://127.0.0.1:8000/api/v1/authors"

# Получить все категории
curl "http://127.0.0.1:8000/api/v1/categories"
```

---

## 🗄️ Структура базы данных

### Таблица `authors`
- `id` (INTEGER, PRIMARY KEY) - Уникальный идентификатор исполнителя
- `name` (TEXT, NOT NULL) - Имя исполнителя

### Таблица `categories`
- `id` (INTEGER, PRIMARY KEY) - Уникальный идентификатор категории
- `name` (TEXT, NOT NULL) - Название категории

### Таблица `products`
- `id` (INTEGER, PRIMARY KEY) - Уникальный идентификатор пластинки
- `name` (TEXT, NOT NULL) - Название пластинки
- `description` (TEXT) - Описание пластинки
- `price` (REAL, NOT NULL) - Цена пластинки
- `cover_url` (TEXT) - URL обложки
- `author_id` (INTEGER, FOREIGN KEY) - Ссылка на исполнителя

### Таблица `product_categories` (связующая)
- `product_id` (INTEGER, FOREIGN KEY) - Ссылка на пластинку
- `category_id` (INTEGER, FOREIGN KEY) - Ссылка на категорию

---

## 🚀 Запуск всех микросервисов

### Через .bat файлы

```bash
# Запуск всех сервисов
start_all_services.bat

# Запуск отдельных сервисов
start_catalog.bat    # Порт 8001
start_cart.bat       # Порт 8002
start_orders.bat     # Порт 8003
start_users.bat      # Порт 8004

# Остановка всех сервисов
stop_all_services.bat
```

### Порты сервисов

- **Catalog Service:** `http://127.0.0.1:8001`
- **Cart Service:** `http://127.0.0.1:8002`
- **Orders Service:** `http://127.0.0.1:8003`
- **Users Service:** `http://127.0.0.1:8004`

---

## 🧪 Тестирование

### Запуск тестов

```bash
# Запуск всех тестов
python -m pytest tests/test_catalog_service.py -v

# Запуск конкретного теста
python -m pytest tests/test_catalog_service.py::TestProductCRUD::test_create_product -v
```

### Покрытие тестами

Тесты покрывают:
- ✅ Создание пластинок, исполнителей и категорий
- ✅ Получение данных (одиночные записи и списки)
- ✅ Обновление пластинок
- ✅ Удаление пластинок
- ✅ Обработка ошибок (404, валидация)
- ✅ Пагинация
- ✅ Связи между сущностями

---

## 📝 Pydantic схемы

### ProductCreate
```python
{
  "name": str,
  "description": Optional[str],
  "price": float,
  "author_id": int,
  "category_ids": List[int]
}
```

### ProductUpdate
```python
{
  "name": Optional[str],
  "description": Optional[str],
  "price": Optional[float],
  "author_id": Optional[int],
  "category_ids": Optional[List[int]]
}
```

### ProductSchema (ответ)
```python
{
  "id": int,
  "name": str,
  "description": Optional[str],
  "price": float,
  "cover_url": Optional[str],
  "author": AuthorSchema,
  "categories": List[CategorySchema]
}
```

---

## 🔍 Дополнительные возможности

### Автоматическая документация API

FastAPI автоматически генерирует интерактивную документацию:

- **Swagger UI:** `http://127.0.0.1:8001/docs`
- **ReDoc:** `http://127.0.0.1:8001/redoc`

### Валидация данных

Все входящие данные автоматически валидируются с помощью Pydantic:
- Проверка типов данных
- Проверка обязательных полей
- Проверка диапазонов значений (например, `price > 0`)

### Обработка ошибок

Сервис возвращает структурированные ошибки:
```json
{
  "detail": "Описание ошибки"
}
```

---

## 📞 Поддержка

При возникновении проблем проверьте:

1. **База данных инициализирована:** Сервис автоматически создает таблицы при запуске
2. **Порты свободны:** Убедитесь, что порты 8001-8004 не заняты другими процессами
3. **Зависимости установлены:** `pip install -r requirements.txt`
4. **Логи сервиса:** Проверьте вывод консоли на наличие ошибок

Для получения дополнительной помощи обратитесь к логам сервиса или документации FastAPI.




