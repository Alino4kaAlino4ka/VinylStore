# 🚀 Руководство по деплою на production

## Обзор

Это руководство описывает процесс деплоя проекта vinyl-store на production окружение. Все шаги основаны на чеклисте готовности проекта (30/30 пунктов выполнено).

## Предварительные требования

### ✅ Проверка готовности

Убедитесь, что выполнены все пункты из [DEPLOYMENT_CHECKLIST.md](../DEPLOYMENT_CHECKLIST.md):

- ✅ Безопасность (100%)
- ✅ Структура проекта (100%)
- ✅ Тестирование (92% успешность)
- ✅ База данных (100%)
- ✅ Документация (100%)
- ✅ Зависимости (100%)
- ✅ Миграции (100%)
- ✅ Production настройки (скрипты готовы)

### Системные требования

- **Python 3.9+** (рекомендуется 3.12+)
- **MySQL 8.0+** (или PostgreSQL)
- **Node.js** (опционально, для frontend сборки)
- **MySQL Client Tools** (для резервного копирования)

## Шаг 1: Подготовка сервера

### 1.1 Установка зависимостей

```bash
# Обновление системы (Ubuntu/Debian)
sudo apt-get update && sudo apt-get upgrade -y

# Установка Python и pip
sudo apt-get install python3 python3-pip python3-venv -y

# Установка MySQL
sudo apt-get install mysql-server mysql-client -y

# Установка системных зависимостей
sudo apt-get install build-essential libssl-dev libffi-dev -y
```

### 1.2 Настройка MySQL

```bash
# Запуск MySQL
sudo systemctl start mysql
sudo systemctl enable mysql

# Безопасная настройка MySQL
sudo mysql_secure_installation
```

### 1.3 Создание базы данных

```bash
# Войдите в MySQL
sudo mysql -u root -p

# Создайте базу данных и пользователя
CREATE DATABASE audio_store CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'vinyl_user'@'localhost' IDENTIFIED BY 'STRONG_PASSWORD_HERE';
GRANT ALL PRIVILEGES ON audio_store.* TO 'vinyl_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

## Шаг 2: Клонирование и настройка проекта

### 2.1 Клонирование репозитория

```bash
# Клонируйте репозиторий
git clone https://github.com/yourusername/vinyl-store.git
cd vinyl-store

# Или скопируйте файлы проекта на сервер
```

### 2.2 Создание виртуального окружения

```bash
# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows
```

### 2.3 Установка зависимостей

```bash
# Установите зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

### 2.4 Настройка конфигурации

```bash
# Скопируйте пример production конфигурации
cp config/config.env.production.example config.env

# Отредактируйте config.env
nano config.env  # или используйте ваш редактор
```

## Шаг 3: Настройка config.env для production

### 3.1 Обязательные изменения

Откройте `config.env` и выполните следующие изменения:

#### 1. SECRET_KEY

```bash
# Сгенерируйте новый безопасный ключ
python -c "import secrets; print(secrets.token_urlsafe(32))"

# Вставьте сгенерированный ключ в config.env
SECRET_KEY=ваш-сгенерированный-ключ-здесь
```

#### 2. ADMIN_PASSWORD

```bash
# Сгенерируйте безопасный пароль
python -c "import secrets; print(secrets.token_urlsafe(16))"

# В config.env
ADMIN_PASSWORD=ваш-безопасный-пароль-здесь
```

#### 3. DATABASE_URL

```env
# Замените на реальные данные
DATABASE_URL=mysql+pymysql://vinyl_user:STRONG_PASSWORD@localhost/audio_store
```

#### 4. ENVIRONMENT

```env
ENVIRONMENT=production
```

#### 5. CORS

```env
# Укажите конкретные домены (НЕ используйте *)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
```

#### 6. Service URLs

```env
# Замените на реальные домены
CATALOG_SERVICE_URL=https://yourdomain.com:8000
AUTH_SERVICE_URL=https://yourdomain.com:8001
# ... и так далее для всех сервисов
```

### 3.2 Заполнение остальных параметров

Убедитесь, что заполнены все параметры:
- `OPENROUTER_API_KEY` - ваш API ключ OpenRouter
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `TELEGRAM_CHAT_ID` - ID чата для уведомлений
- `SMTP_*` - настройки SMTP для email
- `EMAIL_FROM` и `EMAIL_COPY_TO` - адреса email

## Шаг 4: Инициализация базы данных

### 4.1 Инициализация структуры БД

```bash
# Инициализация таблиц
python scripts/utils/db/init_db.py
```

### 4.2 Заполнение начальными данными (опционально)

```bash
# Заполнение тестовыми данными
python scripts/utils/db/seed_db.py
```

### 4.3 Проверка подключения

```bash
# Проверка подключения к БД
python -c "from database.connection import DATABASE_URL; print('Database URL:', DATABASE_URL)"
```

## Шаг 5: Настройка резервного копирования

### 5.1 Создание директории для бэкапов

```bash
# Создайте директорию
mkdir -p /var/backups/vinyl-store

# Установите права доступа
sudo chown -R $USER:$USER /var/backups/vinyl-store
```

### 5.2 Тестовый бэкап

```bash
# Создайте тестовый бэкап
python scripts/utils/db/backup_db.py --backup-dir /var/backups/vinyl-store
```

### 5.3 Настройка автоматического бэкапа

См. подробную инструкцию в [DATABASE_BACKUP_GUIDE.md](DATABASE_BACKUP_GUIDE.md)

**Быстрый пример (cron):**
```bash
# Добавьте в crontab (ежедневно в 2:00)
crontab -e

# Добавьте строку:
0 2 * * * cd /path/to/project && /path/to/venv/bin/python scripts/utils/db/backup_db.py --backup-dir /var/backups/vinyl-store
```

## Шаг 6: Настройка системы запуска

### 6.1 Создание systemd сервисов (Linux)

Создайте файл `/etc/systemd/system/vinyl-store.service`:

```ini
[Unit]
Description=Vinyl Store Microservices
After=network.target mysql.service

[Service]
Type=simple
User=www-data
WorkingDirectory=/path/to/project
Environment="PATH=/path/to/project/venv/bin"
ExecStart=/path/to/project/venv/bin/python scripts/launch/start_all_services.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активируйте сервис:
```bash
sudo systemctl daemon-reload
sudo systemctl enable vinyl-store
sudo systemctl start vinyl-store
```

### 6.2 Проверка статуса

```bash
# Проверка статуса сервисов
sudo systemctl status vinyl-store

# Просмотр логов
sudo journalctl -u vinyl-store -f
```

## Шаг 7: Настройка веб-сервера (Nginx)

### 7.1 Установка Nginx

```bash
sudo apt-get install nginx -y
```

### 7.2 Конфигурация Nginx

Создайте файл `/etc/nginx/sites-available/vinyl-store`:

```nginx
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;

    # Редирект на HTTPS (после настройки SSL)
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name yourdomain.com www.yourdomain.com;

    # SSL сертификаты (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;

    # Frontend (статический контент)
    location / {
        root /path/to/project/src;
        try_files $uri $uri/ /index.html;
    }

    # API endpoints (проксирование на микросервисы)
    location /api/v1/catalog/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/v1/auth/ {
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # ... аналогично для остальных сервисов
}
```

### 7.3 Активация конфигурации

```bash
# Создайте симлинк
sudo ln -s /etc/nginx/sites-available/vinyl-store /etc/nginx/sites-enabled/

# Проверьте конфигурацию
sudo nginx -t

# Перезапустите Nginx
sudo systemctl restart nginx
```

## Шаг 8: Настройка SSL (HTTPS)

### 8.1 Установка Certbot

```bash
sudo apt-get install certbot python3-certbot-nginx -y
```

### 8.2 Получение сертификата

```bash
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com
```

### 8.3 Автоматическое обновление

Certbot автоматически настроит обновление сертификатов через cron.

## Шаг 9: Настройка файрвола

### 9.1 Настройка UFW (Ubuntu)

```bash
# Разрешить SSH
sudo ufw allow 22/tcp

# Разрешить HTTP и HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Включить файрвол
sudo ufw enable
```

### 9.2 Проверка портов

Убедитесь, что порты микросервисов (8000-8012) доступны только локально:
```bash
# Проверьте, что порты не открыты наружу
sudo netstat -tlnp | grep -E ':(8000|8001|8005|8007|8010|8011|8012)'
```

## Шаг 10: Финальная проверка

### 10.1 Проверка сервисов

```bash
# Проверьте health endpoints
curl http://127.0.0.1:8000/health
curl http://127.0.0.1:8001/health
# ... и так далее для всех сервисов
```

### 10.2 Проверка фронтенда

Откройте в браузере:
- https://yourdomain.com
- Проверьте работу каталога
- Проверьте авторизацию
- Проверьте корзину
- Проверьте оформление заказа

### 10.3 Запуск тестов

```bash
# Запустите комплексные тесты
python tests/test_full_suite.py
```

## Шаг 11: Мониторинг и обслуживание

### 11.1 Настройка мониторинга

Рекомендуется настроить:
- Мониторинг доступности сервисов
- Мониторинг использования ресурсов
- Мониторинг логов на ошибки
- Алерты при критических проблемах

### 11.2 Логирование

Логи сохраняются в `logs/` директории:
- `logs/catalog_stdout.log`
- `logs/auth_stdout.log`
- ... и так далее для каждого сервиса

### 11.3 Регулярное обслуживание

- **Ежедневно:** Проверка бэкапов
- **Еженедельно:** Проверка логов на ошибки
- **Ежемесячно:** Обновление зависимостей
- **По необходимости:** Обновление системы безопасности

## Чеклист перед запуском

- [ ] Все изменения в `config.env` выполнены
- [ ] SECRET_KEY сгенерирован новый
- [ ] ADMIN_PASSWORD изменен на безопасный
- [ ] ENVIRONMENT=production установлен
- [ ] CORS настроен для конкретных доменов
- [ ] DATABASE_URL настроен правильно
- [ ] База данных инициализирована
- [ ] Резервное копирование настроено
- [ ] Systemd сервисы созданы и запущены
- [ ] Nginx настроен и работает
- [ ] SSL сертификаты установлены
- [ ] Файрвол настроен
- [ ] Все health endpoints отвечают
- [ ] Фронтенд доступен и работает
- [ ] Тесты пройдены успешно

## Дополнительные ресурсы

- [DEPLOYMENT_CHECKLIST.md](../DEPLOYMENT_CHECKLIST.md) - полный чеклист деплоя
- [DEPLOYMENT_READY.md](../docs/DEPLOYMENT_READY.md) - отчет о готовности
- [DATABASE_BACKUP_GUIDE.md](DATABASE_BACKUP_GUIDE.md) - руководство по бэкапам
- [SECURITY.md](../SECURITY.md) - документация по безопасности

## Поддержка

При возникновении проблем:
1. Проверьте логи в `logs/` директории
2. Проверьте статус сервисов: `sudo systemctl status vinyl-store`
3. Проверьте конфигурацию: `python scripts/utils/db/init_db.py`
4. Обратитесь к документации в `docs/`

---

**Дата создания:** 2025-11-05  
**Версия:** 1.0

