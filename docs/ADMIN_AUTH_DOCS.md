# 🔐 Система авторизации администраторов

## Обзор

Система авторизации администраторов обеспечивает безопасный доступ к админ-панели для управления контентом интернет-магазина аудиокниг. Система включает в себя проверку учетных данных, генерацию токенов и защиту от несанкционированного доступа.

## Архитектура

### Компоненты системы

1. **`src/admin_config.py`** - Основной модуль конфигурации
2. **`src/admin/login.html`** - Страница входа для администраторов
3. **`src/admin/admin.html`** - Админ-панель с проверкой авторизации
4. **`src/admin/admin.js`** - JavaScript функции для работы с токенами

### Поток авторизации

```mermaid
graph TD
    A[Пользователь] --> B[Страница входа]
    B --> C{Проверка учетных данных}
    C -->|Валидны| D[Генерация токена]
    C -->|Невалидны| E[Ошибка авторизации]
    D --> F[Сохранение токена в localStorage]
    F --> G[Перенаправление на админ-панель]
    G --> H[Проверка токена при загрузке]
    H -->|Токен валиден| I[Доступ к админ-панели]
    H -->|Токен невалиден| J[Перенаправление на страницу входа]
    I --> K[Кнопка "Выйти"]
    K --> L[Очистка токена]
    L --> B
```

## Конфигурация

### Учетные данные по умолчанию

```python
ADMIN_CREDENTIALS = {
    "username": "admin",
    "password": "admin123"
}
```

### Функции конфигурации

#### `verify_admin_credentials(username, password)`
Проверяет учетные данные администратора.

**Параметры:**
- `username` (str): Имя пользователя
- `password` (str): Пароль

**Возвращает:**
- `bool`: True если учетные данные верны, False в противном случае

#### `generate_admin_token()`
Генерирует уникальный токен для администратора.

**Возвращает:**
- `str`: Сгенерированный токен (SHA256 хеш)

#### `verify_admin_token(token)`
Проверяет валидность токена администратора.

**Параметры:**
- `token` (str): Токен для проверки

**Возвращает:**
- `bool`: True если токен валиден, False в противном случае

## JavaScript API

### Функции авторизации

#### `checkAdminToken()`
Проверяет наличие и валидность токена в localStorage.

**Возвращает:**
- `bool`: True если токен валиден, False в противном случае

#### `logoutAdmin()`
Выполняет выход из системы администратора.

**Действия:**
1. Удаляет токен из localStorage
2. Перенаправляет на страницу входа

## Безопасность

### Меры защиты

1. **Проверка учетных данных**
   - Строгое сравнение имени пользователя и пароля
   - Чувствительность к регистру

2. **Генерация токенов**
   - Использование криптографически стойких алгоритмов
   - Уникальность каждого токена
   - Временные метки для предотвращения повторного использования

3. **Проверка токенов**
   - Валидация формата токена
   - Проверка длины и структуры
   - Защита от подделки

4. **Защита сессий**
   - Автоматическое перенаправление при отсутствии токена
   - Очистка токена при выходе
   - Проверка токена при каждой загрузке страницы

### Рекомендации по безопасности

1. **Изменение паролей по умолчанию**
   ```python
   ADMIN_CREDENTIALS = {
       "username": "your_admin_username",
       "password": "your_secure_password"
   }
   ```

2. **Использование HTTPS**
   - Всегда используйте HTTPS в продакшене
   - Токены передаются через защищенное соединение

3. **Регулярная ротация токенов**
   - Реализуйте автоматическое обновление токенов
   - Установите время жизни токенов

## Тестирование

### Типы тестов

1. **Unit-тесты Python** (`tests/test_admin_auth.py`)
   - Тестирование функций конфигурации
   - Проверка валидности учетных данных
   - Тестирование генерации токенов
   - Проверка безопасности

2. **Unit-тесты JavaScript** (`tests/test_admin_auth_unit.js`)
   - Тестирование функций авторизации в браузере
   - Проверка работы с localStorage
   - Тестирование DOM элементов
   - Проверка обработчиков событий

3. **Интерактивные тесты** (`test_admin_auth.html`)
   - Визуальный интерфейс для тестирования
   - Тестирование полного потока авторизации
   - Проверка безопасности и граничных случаев
   - Интеграционные тесты

### Запуск тестов

```bash
# Python unit-тесты
cd tests
python test_admin_auth.py

# Интерактивные тесты
start test_admin_auth.html
```

### Сценарии тестирования

1. **Позитивные тесты**
   - Успешная авторизация с валидными данными
   - Генерация и проверка токенов
   - Корректный выход из системы

2. **Негативные тесты**
   - Авторизация с неверными учетными данными
   - Попытка доступа без токена
   - Использование невалидных токенов

3. **Тесты безопасности**
   - Попытка подбора паролей
   - Проверка чувствительности к регистру
   - Тестирование с пустыми значениями
   - Проверка валидации специальных символов

## Развертывание

### Требования

- Python 3.6+
- Современный веб-браузер с поддержкой localStorage
- Веб-сервер (для продакшена)

### Установка

1. Скопируйте файлы системы авторизации в проект
2. Настройте учетные данные в `admin_config.py`
3. Убедитесь, что все зависимости установлены
4. Запустите тесты для проверки работоспособности

### Конфигурация продакшена

1. **Изменение учетных данных**
   ```python
   ADMIN_CREDENTIALS = {
       "username": "production_admin",
       "password": "secure_production_password"
   }
   ```

2. **Настройка HTTPS**
   - Используйте SSL-сертификаты
   - Настройте принудительное перенаправление на HTTPS

3. **Мониторинг**
   - Логируйте попытки авторизации
   - Отслеживайте подозрительную активность
   - Настройте уведомления о неудачных попытках входа

## Устранение неполадок

### Частые проблемы

1. **"Токен не найден"**
   - Проверьте, что localStorage поддерживается браузером
   - Убедитесь, что токен не был очищен

2. **"Неверные учетные данные"**
   - Проверьте правильность ввода имени пользователя и пароля
   - Убедитесь, что учетные данные не изменены в конфигурации

3. **"Перенаправление не работает"**
   - Проверьте правильность путей к файлам
   - Убедитесь, что JavaScript включен в браузере

### Отладка

1. **Проверка токена в консоли браузера**
   ```javascript
   console.log(localStorage.getItem('admin_token'));
   ```

2. **Проверка функций авторизации**
   ```javascript
   console.log(checkAdminToken());
   ```

3. **Очистка токена для тестирования**
   ```javascript
   localStorage.removeItem('admin_token');
   ```

## Обновления и поддержка

### Версионирование

- **v1.0.0** - Базовая система авторизации
- **v1.1.0** - Добавлены тесты безопасности
- **v1.2.0** - Улучшена генерация токенов

### Планы развития

1. **Двухфакторная аутентификация**
2. **Интеграция с внешними системами авторизации**
3. **Расширенное логирование и мониторинг**
4. **Поддержка множественных администраторов**

### Поддержка

Для получения поддержки или сообщения об ошибках:
1. Проверьте документацию по тестированию
2. Запустите тесты для диагностики проблем
3. Обратитесь к разработчикам с подробным описанием проблемы
