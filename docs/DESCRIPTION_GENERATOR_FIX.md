# Исправление AI генератора описаний в админ-панели

## Проблема
Генератор описаний в админ-панели (`src/admin/admin.html`) не работал из-за:
1. ❌ Превышено время ожидания (45 сек) - таймаут был слишком коротким для LLM запросов
2. ❌ Неправильный HTTP метод (PATCH вместо PUT)
3. ❌ Неправильный эндпоинт для обновления описания
4. ❌ Несоответствие портов в `start_services_final.py`

## Исправления

### 1. Увеличен таймаут на фронтенде
**Файл:** `src/admin/admin.js`
- Увеличен таймаут с 45 секунд до 120 секунд (2 минуты)
- Обновлены сообщения об ошибках

### 2. Исправлен эндпоинт в recommender service
**Файл:** `services/recommender/main.py`
- Изменен метод с `PATCH` на `PUT`
- Исправлен путь с `/api/v1/products/{id}` на `/api/v1/admin/products/{id}`
- Добавлена обработка ошибок подключения к catalog API
- Оптимизирован LLM запрос (уменьшено max_tokens с 400 до 300)

### 3. Исправлены порты в start_services_final.py
**Файл:** `start_services_final.py`
- Catalog: порт 8000 ✅
- Cart: порт 8001 ✅
- Orders: порт 8002 ✅
- Users: порт 8003 ✅
- Recommender: порт 8004 ✅

### 4. Улучшена обработка ошибок
- Добавлены понятные сообщения об ошибках
- Обработка таймаутов на разных этапах
- Проверка доступности catalog API

## Важное замечание

⚠️ **Генератор описаний работает только с товарами, которые существуют в catalog API (порт 8000), а не только в localStorage админ-панели.**

Если товар существует только в localStorage, он не будет найден при генерации описания. Убедитесь, что товары синхронизированы с catalog API перед использованием генератора.

## Порты сервисов

| Сервис | Порт | Эндпоинт |
|--------|------|----------|
| Catalog | 8000 | `/api/v1/products`, `/api/v1/admin/products` |
| Cart | 8001 | `/api/v1/cart/...` |
| Orders | 8002 | `/api/v1/orders/...` |
| Users | 8003 | `/api/v1/users/...` |
| Recommender | 8004 | `/api/v1/recommendations/generate-description/{id}` |

## Запуск сервисов

```bash
# Вариант 1: Используя start_all_services.bat
start_all_services.bat

# Вариант 2: Используя Python скрипт (исправленный)
python start_services_final.py
```

## Тестирование

1. Убедитесь, что все сервисы запущены
2. Откройте админ-панель: `src/admin/admin.html`
3. Отредактируйте существующий товар (который есть в catalog API)
4. Нажмите кнопку "🤖 AI Генератор"
5. Дождитесь генерации (может занять до 2 минут)

## Возможные проблемы

### Таймаут всё ещё возникает
- Проверьте подключение к интернету (OpenRouter API)
- Проверьте OPENROUTER_API_KEY в `config.env`
- Попробуйте позже (OpenRouter может быть перегружен)

### Товар не найден
- Убедитесь, что товар существует в catalog API на порту 8000
- Проверьте, что catalog service запущен
- Используйте публичный API для проверки: `http://127.0.0.1:8000/api/v1/products/{id}`

### Ошибка подключения к catalog API
- Проверьте, что catalog service запущен на порту 8000
- Проверьте логи сервиса на наличие ошибок

