# 🆕 Новый функционал Vinyl Shop

## Обзор добавленных возможностей

Документация описывает все новые функции, добавленные в систему Vinyl Shop.

---

## 📧 Email уведомления

### Описание
При оформлении заказа система автоматически отправляет email пользователю с деталями заказа, мнением музыкального эксперта и персональными рекомендациями.

### Функциональность

#### 1. Email пользователю
- **Получатель:** Email пользователя, указанный при регистрации
- **Тема:** `Ваш заказ №{order_id} - Винил Шоп`
- **Содержимое:**
  - Информация о заказе (номер, дата, товары)
  - Мнение музыкального эксперта о выборе
  - Персональные рекомендации (до 3 штук)
  - Таблица товаров с ценами
  - Общая сумма заказа

#### 2. Дубликат email администратору
- **Получатель:** Адрес, указанный в `EMAIL_COPY_TO` (по умолчанию `EMAIL_FROM`)
- **Тема:** `[ДУБЛИКАТ] Заказ №{order_id} от {user_email} - Винил Шоп`
- **Содержимое:** То же, что и пользователю + **Email заказчика** в информации о заказе
- **Условие:** Отправляется только если `EMAIL_COPY_TO` отличается от email пользователя

### Конфигурация

В `config.env`:
```env
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password
EMAIL_FROM=your_email@gmail.com
EMAIL_COPY_TO=your_email@gmail.com  # Адрес для дубликатов
```

### Технические детали

- **Формат:** HTML
- **Кодировка:** UTF-8
- **Безопасность:** Используется пароль приложения Gmail
- **Обработка ошибок:** Ошибки логируются, но не блокируют создание заказа

---

## 📱 Telegram уведомления

### Описание
При создании нового заказа администратор получает уведомление в Telegram со всей информацией о заказе и пользователе.

### Функциональность

#### Содержимое сообщения
- Номер заказа
- Дата и время заказа
- Email пользователя
- Список товаров с ценами и количеством
- Общая сумма заказа
- Предупреждение, если Catalog service недоступен

### Конфигурация

В `config.env`:
```env
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

### Получение данных

1. **Bot Token:**
   - Создайте бота через [@BotFather](https://t.me/BotFather)
   - Скопируйте полученный токен

2. **Chat ID:**
   - Напишите боту [@userinfobot](https://t.me/userinfobot)
   - Или используйте [@getidsbot](https://t.me/getidsbot)
   - Скопируйте ваш Chat ID

### Технические детали

- **API:** Telegram Bot API
- **Формат:** Markdown
- **Обработка ошибок:** Ошибки логируются, но не блокируют создание заказа

---

## 🎵 Мнение музыкального эксперта

### Описание
При оформлении заказа AI-система генерирует профессиональное мнение музыкального эксперта о выборе пользователя. Это мнение отправляется в email пользователю.

### Функциональность

#### Генерация мнения
- **Сервис:** Recommender Service (8012)
- **Модель:** GPT-4o-mini (по умолчанию)
- **Промпт:** Профессиональное мнение эксперта о выборе пластинок
- **Формат:** 2-3 предложения, профессиональное, но вдохновляющее

#### Отображение
- **В email:** Раздел "🎵 Мнение музыкального эксперта"
- **Стиль:** Выделенный блок с профессиональным оформлением

### Технические детали

- **Интеграция:** Orders Service → Recommender Service
- **Промпт:** Динамически формируется на основе купленных товаров
- **Fallback:** Если генерация не удалась, раздел не отображается

---

## 💡 Персональные рекомендации

### Описание
На основе покупки пользователя система генерирует до 3 персональных рекомендаций виниловых пластинок и отправляет их в email.

### Функциональность

#### Генерация рекомендаций
- **Сервис:** Recommender Service (8012)
- **Модель:** GPT-4o-mini (по умолчанию)
- **Основа:** Купленные товары
- **Количество:** До 3 рекомендаций

#### Формат рекомендации
- Название пластинки
- Исполнитель
- Причина рекомендации (описание)
- Оценка совпадения (match_score)

#### Отображение
- **В email:** Раздел "💡 Рекомендации для вас"
- **Стиль:** Красивые карточки с нумерацией
- **Ограничения:** Длина описания до 200 символов

### Технические детали

- **Интеграция:** Orders Service → Recommender Service → Catalog Service
- **Запрос:** Передается список ID купленных товаров
- **Fallback:** Если генерация не удалась, используется простой промпт
- **Обработка:** Нормализация формата ответа для разных вариантов API

---

## 🔐 Обязательная авторизация для заказов

### Описание
Оформление заказа теперь доступно только зарегистрированным и авторизованным пользователям.

### Функциональность

#### Проверка авторизации
- **Требование:** Обязательный заголовок `Authorization: Bearer {token}`
- **Проверка:** Orders Service → Auth Service
- **Результат:** Если не авторизован, возвращается ошибка 401

#### Frontend поведение
- **Проверка:** Перед оформлением проверяется наличие токена в `localStorage`
- **Действие:** Если токена нет, пользователь перенаправляется на страницу входа
- **Сообщение:** Дружелюбное сообщение о необходимости авторизации
- **Сохранение:** Корзина сохраняется в localStorage

### Обработка ошибок

#### На Backend
- **401 Unauthorized:** "Не удалось подтвердить вашу авторизацию. Для оформления заказа необходимо войти в систему. Мы сохранили вашу корзину - просто войдите и попробуйте еще раз."

#### На Frontend
- **401 Unauthorized:** "Ваша сессия истекла или требуется авторизация. Ваша корзина сохранена! Пожалуйста, войдите в систему снова."
- **Другие ошибки:** Дружелюбные сообщения с сохранением корзины

### Технические детали

- **JWT токены:** Используются для авторизации
- **Валидация:** Проверка токена через Auth Service
- **Информация пользователя:** Получается из Auth Service для email и Telegram

---

## 🔧 Обновление портов

### Изменения портов

| Сервис | Старый порт | Новый порт | Причина |
|--------|------------|-----------|---------|
| Orders | 8002 | 8010 | Конфликт портов |
| Users | 8003 | 8011 | Конфликт портов |
| Recommender | 8004 | 8012 | Конфликт портов |

### Обновленные файлы

- `config.env` - обновлены порты и URLs
- `start_services_final.py` - обновлены порты запуска
- `src/config/api-config.js` - обновлены URL для frontend
- `check_services_status.py` - обновлены порты для проверки
- `PORTS_CONFIG.md` - обновлена документация
- Все сервисы (`main.py`) - обновлены порты

---

## 🔒 Безопасность и конфиденциальные данные

### Защита секретов

#### Файлы в .gitignore
- `config.env` - основной файл с секретами
- `.env`, `*.env` - все варианты env файлов
- `*.db` - базы данных
- `logs/` - логи

#### Созданные файлы
- `config.env.example` - шаблон без секретов
- `SECURITY.md` - подробная инструкция по безопасности

#### Переменные окружения
- `OPENROUTER_API_KEY` - ключ AI API
- `SECRET_KEY` - ключ для JWT
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `TELEGRAM_CHAT_ID` - ID чата Telegram
- `SMTP_USERNAME` - email для SMTP
- `SMTP_PASSWORD` - пароль приложения для SMTP
- `ADMIN_USERNAME` - логин администратора
- `ADMIN_PASSWORD` - пароль администратора

### Рекомендации

1. **Перед коммитом:** Проверьте, что `config.env` не в Git
2. **Генерация SECRET_KEY:** Используйте команду из `SECURITY.md`
3. **Production:** Измените все дефолтные значения
4. **Email:** Используйте пароли приложений, не основные пароли

---

## 📊 Интеграция компонентов

### Схема взаимодействия при создании заказа

```
Frontend (cart.js)
    ↓
Orders Service (8010)
    ├─→ Auth Service (8001) - проверка авторизации
    ├─→ Catalog Service (8000) - получение информации о товарах
    ├─→ Recommender Service (8012) - генерация мнения эксперта
    ├─→ Recommender Service (8012) - генерация рекомендаций
    ├─→ SMTP Server - отправка email пользователю
    ├─→ SMTP Server - отправка дубликата email
    └─→ Telegram Bot API - отправка уведомления
```

### Зависимости

1. **Orders Service** зависит от:
   - Auth Service (авторизация)
   - Catalog Service (информация о товарах)
   - Recommender Service (AI функции)

2. **Recommender Service** зависит от:
   - Prompts Manager Service (промпты)
   - Catalog Service (каталог пластинок)

3. **Email отправка** требует:
   - Настроенный SMTP сервер
   - Пароль приложения Gmail

4. **Telegram уведомления** требуют:
   - Токен бота
   - Chat ID

---

## 🎨 Форматирование сообщений

### Email формат

#### Структура
1. **Заголовок:** "🎵 Спасибо за ваш заказ!"
2. **Информация о заказе:**
   - Номер заказа
   - Дата и время
   - Количество товаров
   - Email заказчика (только в дубликате)
3. **Мнение эксперта:** Профессиональное мнение о выборе
4. **Рекомендации:** До 3 персональных рекомендаций
5. **Таблица товаров:** Детальная информация
6. **Итоговая сумма**

#### Стилизация
- Градиентный заголовок
- Карточки для рекомендаций
- Таблица с четким форматированием
- Адаптивный дизайн

### Telegram формат

#### Структура
- Заголовок с номером заказа
- Email пользователя
- Список товаров с форматированием
- Общая сумма
- Предупреждения (если есть)

---

## 🐛 Обработка ошибок

### Стратегия обработки

1. **Генерация мнения эксперта:**
   - Ошибка не блокирует создание заказа
   - Раздел просто не отображается в email
   - Ошибка логируется

2. **Генерация рекомендаций:**
   - Пробуется основной метод
   - Если не работает, используется fallback с простым промптом
   - Ошибка не блокирует создание заказа
   - Ошибка логируется

3. **Отправка email:**
   - Ошибка не блокирует создание заказа
   - Ошибка логируется с traceback

4. **Отправка Telegram:**
   - Ошибка не блокирует создание заказа
   - Ошибка логируется с traceback

5. **Catalog Service недоступен:**
   - Используются заглушки для товаров
   - В Telegram добавляется предупреждение
   - Заказ все равно создается

### Логирование

Все действия логируются в `logs/orders_stdout.log`:
- ✅ Успешные операции
- ⚠️ Предупреждения
- ❌ Ошибки
- 📊 Статистика

---

## 🔄 API Endpoints

### Новые/Обновленные endpoints

#### Orders Service (8010)

**POST /api/v1/orders**
- **Требования:** Authorization header (Bearer token)
- **Тело запроса:**
  ```json
  {
    "product_ids": ["1", "2"],
    "quantities": {"1": 1, "2": 2}
  }
  ```
- **Ответ:**
  ```json
  {
    "order_id": "uuid",
    "message": "Order created successfully",
    "created_at": "2025-11-05T01:00:00",
    "product_ids": ["1", "2"],
    "quantities": {"1": 1, "2": 2},
    "total_items": 3
  }
  ```
- **Побочные эффекты:**
  - Генерация мнения эксперта
  - Генерация рекомендаций
  - Отправка email пользователю
  - Отправка дубликата email
  - Отправка Telegram уведомления

---

## 📝 Конфигурация

### Переменные окружения

#### Email (SMTP)
```env
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password
EMAIL_FROM=your_email@gmail.com
EMAIL_COPY_TO=your_email@gmail.com  # Для дубликатов
```

#### Telegram
```env
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

#### Порты
```env
ORDERS_PORT=8010
USERS_PORT=8011
RECOMMENDER_PORT=8012
```

#### URLs для межсервисного взаимодействия
```env
ORDERS_SERVICE_URL=http://127.0.0.1:8010
USERS_SERVICE_URL=http://127.0.0.1:8011
RECOMMENDER_SERVICE_URL=http://127.0.0.1:8012
```

---

## 🧪 Тестирование

### Автоматизированные тесты

Запуск:
```bash
python tests/test_comprehensive.py
```

**Результат:** 16/16 тестов пройдено (100%)

### Чеклист ручного тестирования

См. `tests/TEST_CHECKLIST.md`

### Тестируемые функции

- ✅ Создание заказа с авторизацией
- ✅ Проверка без авторизации (ошибка)
- ✅ Генерация мнения эксперта
- ✅ Генерация рекомендаций
- ✅ Отправка email
- ✅ Отправка Telegram

---

## 📚 Дополнительная документация

- `SECURITY.md` - Безопасность и защита секретов
- `PORTS_CONFIG.md` - Конфигурация портов
- `tests/TEST_CHECKLIST.md` - Чеклист тестирования
- `tests/TEST_RESULTS.md` - Результаты тестирования
- `tests/README_TESTING.md` - Руководство по тестированию

---

## 🚀 Миграция и обновление

### Для существующих проектов

1. **Обновите порты:**
   - Orders: 8002 → 8010
   - Users: 8003 → 8011
   - Recommender: 8004 → 8012

2. **Добавьте переменные окружения:**
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`
   - `EMAIL_COPY_TO`
   - Обновите SMTP настройки

3. **Обновите конфигурацию frontend:**
   - `src/config/api-config.js` - обновите порты

4. **Перезапустите сервисы:**
   ```bash
   python start_services_final.py
   ```

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи в `logs/orders_stdout.log`
2. Убедитесь, что все сервисы запущены
3. Проверьте конфигурацию в `config.env`
4. Запустите тесты: `python tests/test_comprehensive.py`

---

**Последнее обновление:** 2025-11-05  
**Версия:** 1.0.0

