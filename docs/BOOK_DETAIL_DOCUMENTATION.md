# Документация: Страница детальной информации о пластинке (book-detail.js)

## 📖 Обзор

Модуль `book-detail.js` обеспечивает динамическую загрузку и отображение детальной информации о виниловой пластинке на странице `book-detail.html`. Скрипт автоматически получает ID пластинки из URL-параметров, загружает данные через API и создает полную карточку пластинки с обложкой, описанием, рейтингом и кнопками управления.

## 🎯 Основные возможности

- ✅ Автоматическая загрузка данных пластинки по ID из URL
- ✅ Динамическое создание DOM-структуры карточки
- ✅ Обработка ошибок (404, проблемы сети, неверный формат)
- ✅ Интеграция с корзиной (добавление товаров)
- ✅ Обновление счетчика корзины
- ✅ Уведомления пользователю о действиях
- ✅ Поддержка индикатора загрузки

## 🔌 API интеграция

### Конфигурация

```javascript
const API_BASE_URL = 'http://localhost:8000';
const PRODUCTS_ENDPOINT = `${API_BASE_URL}/api/v1/products`;
```

### Эндпоинт

**GET** `/api/v1/products/{id}`

**Пример запроса:**
```javascript
fetch('http://localhost:8000/api/v1/products/1')
```

**Формат ответа API:**
```json
{
  "id": 1,
  "name": "Название пластинки",
  "author": "Имя исполнителя",
  "author_id": 14,
  "description": "Описание пластинки",
  "price": 24.99,
  "cover_url": "https://example.com/cover.jpg"
}
```

## 📝 Структура данных

### Входящие данные (от API)

| Поле | Тип | Обязательное | Описание |
|------|-----|--------------|----------|
| `id` | number | ✅ | Уникальный идентификатор пластинки |
| `name` | string | ✅ | Название пластинки |
| `author` | string | ✅ | Имя исполнителя (строка) |
| `description` | string | ⚪ | Описание пластинки |
| `price` | number | ✅ | Цена в рублях |
| `cover_url` | string | ⚪ | URL обложки пластинки |
| `author_id` | number | ⚪ | ID исполнителя |

**Примечание:** Скрипт поддерживает обратную совместимость:
- Если приходит `title` вместо `name` - используется `title`
- Если приходит `cover_image_url` вместо `cover_url` - используется `cover_image_url`
- Если `author` - объект с полем `name` - извлекается `author.name`

## 🛠️ Основные функции

### `loadBookDetails(bookId)`

Асинхронная функция для загрузки детальной информации о пластинке.

**Параметры:**
- `bookId` (string/number) - ID пластинки для загрузки

**Процесс:**
1. Выполняет GET-запрос к API
2. Обрабатывает ошибки (404, сетевые ошибки)
3. Очищает контейнер `main.page-content`
4. Создает и добавляет карточку пластинки
5. Показывает ошибку при неудаче

**Пример:**
```javascript
await loadBookDetails('1');
```

### `createBookDetailCard(bookData)`

Создает полную DOM-структуру карточки пластинки.

**Параметры:**
- `bookData` (object) - Данные пластинки от API

**Возвращает:**
- `HTMLElement` - Секция `<section class="book-details">` с полной карточкой

**Структура создаваемой карточки:**

```
<section class="book-details">
  <div class="book-cover-section">
    <div class="cover-container">
      <div class="book-cover-image">
        <img src="..." alt="...">
        <div class="play-overlay">...</div>
      </div>
      <div class="store-name">AudioStore</div>
    </div>
    <div class="audio-player">
      <span class="player-label">Слушать фрагмент</span>
      <div class="player-controls">...</div>
      <button class="buy-btn-large">В корзину</button>
    </div>
  </div>
  <div class="book-info-section">
    <div class="title-section">
      <h1>Название</h1>
      <div class="title-actions">...</div>
    </div>
    <div class="meta-info">...</div>
    <div class="rating-section">...</div>
    <p class="description">...</p>
    <div class="social-links">...</div>
    <button class="desktop-btn">Отправить на рабочий стол</button>
  </div>
</section>
```

**Особенности:**
- Обновляет `document.title` на название пластинки
- Генерирует рейтинг, если его нет в API
- Обрезает длинные описания до 500 символов
- Добавляет обработчик клика для кнопки "В корзину"

### `addToCartFromDetail(bookId, bookData)`

Добавляет пластинку в корзину со страницы детализации.

**Параметры:**
- `bookId` (string/number) - ID пластинки
- `bookData` (object) - Полные данные пластинки

**Функциональность:**
1. Нормализует ID (преобразует в строку)
2. Использует `window.addToCartWithQuantity()` если доступна
3. Fallback на старую систему localStorage
4. Обновляет счетчик корзины
5. Показывает уведомление

**Пример:**
```javascript
addToCartFromDetail(1, {
  id: 1,
  name: "Название",
  price: 199.99,
  cover_url: "https://..."
});
```

### `updateCartCount()`

Обновляет счетчик товаров в корзине в header.

**Поддерживаемые системы:**
- Новая система с `window.getCartWithQuantity()`
- Старая система через `localStorage.getItem('cart')`
- Вызов `window.updateCartCount()` если доступен

### `showNotification(message)`

Показывает временное уведомление пользователю.

**Параметры:**
- `message` (string) - Текст уведомления

**Особенности:**
- Отображается 3 секунды
- Анимация появления/исчезновения
- Стиль: оранжевый фон, темный текст
- Позиция: правый верхний угол
- Автоматическое удаление дубликатов

### `showError(message)`

Отображает сообщение об ошибке на странице.

**Параметры:**
- `message` (string) - Текст ошибки

**Отображает:**
```html
<div>
  <h2>Ошибка</h2>
  <p>{message}</p>
</div>
```

## 🚀 Использование

### Базовое использование

1. **Откройте страницу с параметром ID:**
   ```
   book-detail.html?id=1
   ```

2. **Скрипт автоматически:**
   - Получит ID из URL
   - Загрузит данные пластинки
   - Отобразит карточку

### Инициализация

Скрипт автоматически запускается при событии `DOMContentLoaded`:

```javascript
document.addEventListener('DOMContentLoaded', async function() {
    // Автоматическая инициализация
});
```

### Индикатор загрузки

Если на странице есть элемент с `id="loading-indicator"`, скрипт автоматически показывает/скрывает его:

```html
<div id="loading-indicator" style="display: none;">
  Загрузка...
</div>
```

## 🔗 Интеграция с другими модулями

### Корзина

Скрипт интегрируется с системой корзины:

- Использует `window.addToCartWithQuantity()` если доступна
- Использует `window.getCartWithQuantity()` для счетчика
- Fallback на `localStorage` для совместимости

**Требуемые зависимости:**
- `cart.js` (для новой системы корзины)
- `script.js` (для базовых функций корзины)

### Другие скрипты

Скрипт подключается в `book-detail.html`:

```html
<script src="scripts/script.js"></script>
<script src="scripts/recommendations.js"></script>
<script src="scripts/book-detail.js"></script>
```

**Важно:** Не подключайте `catalog.js` на странице детализации, так как их логика может конфликтовать.

## ⚠️ Обработка ошибок

### Типы ошибок

1. **Отсутствует ID в URL:**
   ```javascript
   // Показывается ошибка: "ID пластинки не указан. Используйте формат: ?id=1"
   ```

2. **Пластинка не найдена (404):**
   ```javascript
   // Показывается ошибка: "Пластинка не найдена"
   ```

3. **Проблемы с сетью:**
   ```javascript
   // Показывается ошибка: "Не удалось загрузить информацию о пластинке: [описание ошибки]"
   ```

4. **Неверный формат данных:**
   - Скрипт использует значения по умолчанию для отсутствующих полей
   - `title`: "Название не указано"
   - `author`: "Автор не указан"
   - `description`: "Описание отсутствует"
   - `cover_url`: placeholder изображение

## 🧪 Тестирование

### Python-тесты

Запуск API-тестов:
```bash
python tests/test_book_detail_api.py
```

**Проверяется:**
- Доступность API
- Получение пластинки по ID
- Обработка 404 ошибок
- Структура ответа API
- Получение списка всех пластинок

### HTML-тесты

Откройте в браузере:
```
tests/test_book_detail.html
```

**Доступные тесты:**
1. Проверка API эндпоинта
2. Тест парсинга URL
3. Тест создания карточки
4. Тест обработки ошибок
5. Тест очистки main элемента
6. Полный интеграционный тест
7. Демонстрация работы

## 🔧 Настройка

### Изменение базового URL API

```javascript
const API_BASE_URL = 'http://your-api-server.com';
```

### Изменение эндпоинта

```javascript
const PRODUCTS_ENDPOINT = `${API_BASE_URL}/api/v1/your-endpoint`;
```

### Кастомизация уведомлений

Измените стили в функции `showNotification()`:

```javascript
notification.style.cssText = `
    position: fixed;
    top: 20px;
    right: 20px;
    background-color: #FF9900; // Цвет фона
    color: #0F1014; // Цвет текста
    // ... другие стили
`;
```

## 📊 Примеры использования

### Программный вызов загрузки пластинки

```javascript
// Загрузка пластинки с ID = 5
await loadBookDetails('5');
```

### Создание карточки с мок-данными

```javascript
const mockData = {
    id: 1,
    name: "Тестовая пластинка",
    author: "Тестовый исполнитель",
    description: "Описание",
    price: 199.99,
    cover_url: "https://example.com/cover.jpg"
};

const card = createBookDetailCard(mockData);
document.querySelector('main').appendChild(card);
```

### Добавление обработчика на кнопку корзины

```javascript
// Обработчик добавляется автоматически в createBookDetailCard()
// Но можно добавить свой:
const buyButton = document.querySelector('.buy-btn-large');
buyButton.addEventListener('click', function() {
    console.log('Пластинка добавлена в корзину!');
});
```

## 🐛 Устранение проблем

### Пластинка не загружается

**Проблема:** Страница не показывает данные пластинки.

**Решения:**
1. Проверьте, что сервис каталога запущен:
   ```bash
   python check_services_status.py
   ```

2. Проверьте консоль браузера на ошибки (F12)

3. Убедитесь, что ID указан в URL: `?id=1`

4. Проверьте доступность API:
   ```bash
   curl http://localhost:8000/api/v1/products/1
   ```

### Ошибка CORS

**Проблема:** "CORS policy" ошибки в консоли.

**Решение:** Убедитесь, что в сервисе каталога настроен CORS middleware.

### Корзина не работает

**Проблема:** Кнопка "В корзину" не добавляет товар.

**Решения:**
1. Убедитесь, что подключен `cart.js` или `script.js`
2. Проверьте консоль на ошибки JavaScript
3. Проверьте, что localStorage доступен в браузере

### Карточка не отображается

**Проблема:** Карточка создается, но не видна.

**Решения:**
1. Проверьте, что элемент `main.page-content` существует в HTML
2. Проверьте CSS стили - возможно, элементы скрыты
3. Проверьте консоль на ошибки создания DOM

## 📚 Связанная документация

- [CRUD_API.md](CRUD_API.md) - Документация API сервиса каталога
- [BOOK_DETAIL_FIX_REPORT.md](BOOK_DETAIL_FIX_REPORT.md) - Отчет об исправлениях
- [tests/TEST_BOOK_DETAIL_README.md](tests/TEST_BOOK_DETAIL_README.md) - Документация по тестированию

## 🔄 История изменений

### v1.0.0 (Текущая версия)
- ✅ Базовая загрузка данных пластинки
- ✅ Создание карточки пластинки
- ✅ Интеграция с корзиной
- ✅ Обработка ошибок
- ✅ Уведомления пользователю
- ✅ Индикатор загрузки
- ✅ Поддержка обратной совместимости форматов данных

### Исправления
- Исправлен эндпоинт API: `/api/v1/products` вместо `/api/v1/audiobooks`
- Добавлена поддержка разных форматов данных (name/title, cover_url/cover_image_url)
- Улучшена обработка ошибок
- Добавлены тесты

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12)
2. Запустите тесты: `python tests/test_book_detail_api.py`
3. Откройте HTML-тесты в браузере
4. Проверьте логи сервиса каталога

---

**Версия документации:** 1.0.0  
**Последнее обновление:** 2024

