# 🔧 Устранение неполадок: Кнопка AI-описание

## Проблема: ERR_CONNECTION_REFUSED

### Симптомы
```
POST http://127.0.0.1:8004/api/v1/recommendations/generate-description/1 
net::ERR_CONNECTION_REFUSED
```

### Причина
Сервис рекомендаций не запущен или недоступен на порту 8004.

## Решение

### 1. Проверка статуса сервисов

Запустите скрипт проверки:
```bash
python check_services_status.py
```

Или проверьте вручную:
```bash
curl http://127.0.0.1:8004/health
```

### 2. Запуск сервиса рекомендаций

**Вариант А: Запуск всех сервисов**
```bash
python start_services_final.py
```

**Вариант Б: Запуск только сервиса рекомендаций**
```bash
# Windows
start_recommender.bat

# Linux/Mac
python -m uvicorn services.recommender.main:app --host 127.0.0.1 --port 8004 --reload
```

### 3. Проверка конфигурации

Убедитесь, что в `config.env` указан правильный порт:
```env
RECOMMENDER_PORT=8004
```

### 4. Проверка порта

Проверьте, не занят ли порт 8004 другим процессом:

**Windows:**
```bash
netstat -ano | findstr :8004
```

**Linux/Mac:**
```bash
lsof -i :8004
```

Если порт занят:
- Найдите процесс и остановите его
- Или измените порт в конфигурации

### 5. Проверка API ключа

Убедитесь, что в `config.env` указан `OPENROUTER_API_KEY`:
```env
OPENROUTER_API_KEY=your_key_here
```

## Исправленные проблемы

### ✅ Исправлен порт в новой функции
**Было:** `http://localhost:8003/api/v1/recommendations/generate-description/`  
**Стало:** `http://127.0.0.1:8004/api/v1/recommendations/generate-description/`

### ✅ Улучшена обработка ошибок
Теперь при ошибке подключения показывается понятное сообщение:
> "Не удалось подключиться к сервису. Убедитесь, что сервис рекомендаций запущен на порту 8004."

## Порты сервисов

| Сервис | Порт |
|--------|------|
| Catalog | 8000 |
| Auth | 8001 |
| Orders | 8002 |
| Users | 8003 |
| **Recommender** | **8004** |
| Cart | 8005 |

## Дополнительная диагностика

### Проверка в браузере

1. Откройте консоль разработчика (F12)
2. Перейдите на вкладку Network
3. Попробуйте нажать кнопку "AI-описание"
4. Проверьте запрос к `http://127.0.0.1:8004/api/v1/recommendations/generate-description/`
5. Посмотрите статус ответа:
   - **200 OK** - сервис работает
   - **ERR_CONNECTION_REFUSED** - сервис не запущен
   - **404 Not Found** - неправильный URL
   - **500 Internal Server Error** - ошибка в сервисе

### Логи сервиса

Проверьте логи сервиса рекомендаций:
```bash
# Windows
type logs\recommender_stdout.log
type logs\recommender_stderr.log

# Linux/Mac
cat logs/recommender_stdout.log
cat logs/recommender_stderr.log
```

### Тестирование API вручную

Используйте curl или Postman для проверки API:

```bash
# Проверка health endpoint
curl http://127.0.0.1:8004/health

# Тест генерации описания
curl -X POST http://127.0.0.1:8004/api/v1/recommendations/generate-description/1 \
  -H "Content-Type: application/json"
```

## Быстрая проверка

1. ✅ Сервис рекомендаций запущен на порту 8004
2. ✅ В `config.env` указан `OPENROUTER_API_KEY`
3. ✅ Порты не конфликтуют с другими сервисами
4. ✅ Файрвол не блокирует подключение к localhost:8004
5. ✅ Браузер имеет доступ к `http://127.0.0.1:8004`

## Частые проблемы

### Проблема 1: Сервис запускается, но сразу останавливается
**Решение:** Проверьте логи на наличие ошибок запуска

### Проблема 2: Запрос идет, но долго выполняется
**Решение:** Генерация описания через LLM может занимать 30-90 секунд - это нормально

### Проблема 3: Ошибка 401 или 403
**Решение:** Проверьте правильность `OPENROUTER_API_KEY`

### Проблема 4: Кнопка не реагирует на клик
**Решение:** 
- Проверьте консоль браузера на наличие JavaScript ошибок
- Убедитесь, что кнопка имеет класс `generate-description-btn`
- Проверьте, что обработчик событий установлен

## Контакты для поддержки

Если проблема не решена:
1. Соберите логи из `logs/recommender_*.log`
2. Сделайте скриншот консоли браузера
3. Проверьте статус всех сервисов с помощью `check_services_status.py`

