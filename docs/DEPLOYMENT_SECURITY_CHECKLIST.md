# Чеклист безопасности для деплоя

## ✅ Выполненные исправления безопасности

### 1. Конфигурация и секреты
- ✅ Создан `.gitignore` для исключения `config.env` и других чувствительных файлов
- ✅ Все секреты перенесены в переменные окружения (`config.env`)
- ✅ Удален захардкоженный API ключ из `start_all_services.bat`
- ✅ SECRET_KEY теперь загружается из переменных окружения с предупреждениями

### 2. Безопасность паролей
- ✅ Заменен SHA-256 на bcrypt для хеширования паролей
  - Используется `passlib[bcrypt]` (уже в requirements.txt)
  - Применено в `services/auth/main.py` и `services/catalog/security.py`

### 3. CORS
- ✅ Все сервисы теперь настраивают CORS через переменную окружения `ALLOWED_ORIGINS`
- ✅ Добавлены предупреждения при использовании `allow_origins=["*"]` в production
- ✅ Поддержка списка доменов через запятую

### 4. База данных
- ✅ DATABASE_URL теперь загружается из переменных окружения
- ✅ Поддержка PostgreSQL и MySQL для production через переменную окружения

## ⚠️ КРИТИЧНЫЕ проблемы безопасности

### Admin Panel Authentication (НЕ ИСПРАВЛЕНО)
**КРИТИЧНО:** Админ-авторизация выполняется на клиенте в JavaScript!
- ❌ Учетные данные проверяются в `src/admin/login.html` на стороне клиента
- ❌ Любой может посмотреть исходный код и увидеть логин/пароль
- ⚠️ **РЕКОМЕНДАЦИЯ:** Перенести авторизацию на backend через API endpoint

**Временное решение:**
- ✅ Учетные данные вынесены в переменные окружения (`ADMIN_USERNAME`, `ADMIN_PASSWORD`)
- ⚠️ Но проверка все еще на клиенте - это небезопасно!

## ⚠️ Критичные действия перед деплоем

### 1. Обновить config.env для production

```bash
# Сгенерировать безопасный SECRET_KEY
python -c "import secrets; print(secrets.token_urlsafe(32))"

# Добавить в config.env:
SECRET_KEY=<сгенерированный_ключ>
ENVIRONMENT=production
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
DATABASE_URL=postgresql://user:password@localhost:5432/audio_store
```

### 2. Проверить безопасность API ключей

**КРИТИЧНО:** Убедитесь, что:
- ❌ `config.env` НЕ находится в системе контроля версий (добавлен в `.gitignore`)
- ❌ API ключи НЕ захардкожены в коде
- ❌ API ключи НЕ передаются через командную строку или логи
- ✅ Используются переменные окружения или секреты деплой-платформы

### 3. Настройка CORS для production

В `config.env` для production:
```
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ENVIRONMENT=production
```

**НЕ используйте `ALLOWED_ORIGINS=*` в production!**

### 4. Безопасность базы данных

- Используйте PostgreSQL или MySQL вместо SQLite для production
- Настройте SSL соединение с БД
- Используйте отдельного пользователя БД с минимальными правами
- Регулярно делайте бэкапы

### 5. HTTPS

- Используйте HTTPS для всех сервисов
- Настройте SSL/TLS сертификаты
- Принудительное перенаправление HTTP → HTTPS

### 6. Проверка зависимостей

```bash
# Обновите зависимости и проверьте уязвимости
pip install --upgrade -r requirements.txt
pip install safety
safety check -r requirements.txt
```

### 7. Логирование

- ❌ НЕ логируйте пароли, токены, API ключи
- ✅ Логируйте только необходимую информацию для отладки
- ✅ Настройте ротацию логов

## 📋 Дополнительные рекомендации

### Аутентификация и авторизация
- ✅ Используется JWT токены (HS256)
- ⚠️ Рассмотрите возможность использования RS256 для распределенных систем
- ⚠️ Реализуйте refresh tokens для увеличения безопасности

### Rate Limiting
- ⚠️ Добавьте rate limiting для API endpoints
- ⚠️ Защитите endpoints аутентификации от brute-force атак

### Мониторинг
- ⚠️ Настройте мониторинг подозрительной активности
- ⚠️ Настройте алерты на необычные паттерны запросов

### Резервное копирование
- ⚠️ Настройте автоматическое резервное копирование БД
- ⚠️ Проверьте процедуры восстановления

## 🔒 Проверка перед деплоем

Перед деплоем выполните:

1. ✅ Убедитесь, что `.gitignore` исключает `config.env`
2. ✅ Проверьте, что все секреты в переменных окружения
3. ✅ Генерируйте новый SECRET_KEY для production
4. ✅ Настройте CORS для конкретных доменов
5. ✅ Используйте production БД (PostgreSQL/MySQL)
6. ✅ Включите HTTPS
7. ✅ Проверьте зависимости на уязвимости
8. ✅ Настройте мониторинг и логирование
9. ✅ Проверьте права доступа к файлам на сервере
10. ✅ Настройте firewall правила

## 📝 Пример конфигурации для production

```env
# config.env (НЕ коммитить в git!)

# API Keys
OPENROUTER_API_KEY=<ваш_production_ключ>

# JWT Security
SECRET_KEY=<сгенерированный_32_символьный_ключ>

# Database
DATABASE_URL=postgresql://dbuser:dbpass@dbhost:5432/audio_store

# CORS
ALLOWED_ORIGINS=https://yourdomain.com
ENVIRONMENT=production

# Ports (или используйте переменные окружения деплой-платформы)
CATALOG_PORT=8000
AUTH_PORT=8001
ORDERS_PORT=8002
USERS_PORT=8003
RECOMMENDER_PORT=8004
CART_PORT=8005

# JWT
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

## ⚠️ Важные замечания

1. **Не используйте дефолтные значения** SECRET_KEY в production!
2. **Не публикуйте** config.env в репозиторий
3. **Генерируйте уникальные** SECRET_KEY для каждого окружения
4. **Ограничьте CORS** только нужными доменами
5. **Используйте переменные окружения** платформы деплоя (Heroku, AWS, etc.) для секретов

