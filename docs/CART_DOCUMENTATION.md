# 📦 Документация функциональности корзины

## Обзор

Система корзины позволяет пользователям добавлять товары в корзину из каталога и со страницы детализации пластинки. Корзина синхронизируется с сервером и сохраняется в localStorage для сохранения данных между сеансами.

## Архитектура

### Компоненты системы

1. **`src/scripts/cart.js`** - Основной модуль корзины
   - Управление данными корзины в localStorage
   - Синхронизация с сервером через API
   - Функции добавления/удаления товаров

2. **`src/scripts/catalog.js`** - Интеграция с каталогом
   - Функция `addToCart()` для добавления из каталога
   - Автоматическое обновление счетчика корзины
   - Показ уведомлений при добавлении

3. **`src/scripts/book-detail.js`** - Интеграция со страницей детализации
   - Функция `addToCartFromDetail()` для добавления со страницы детализации
   - Обновление счетчика корзины
   - Уведомления о добавлении

### API корзины

**Базовый URL:** `http://localhost:8002/api/v1/cart`

**Эндпоинты:**
- `GET /cart` - Получить корзину пользователя
- `POST /cart/add` - Добавить товар в корзину
- `POST /cart/remove` - Удалить товар из корзины
- `POST /cart/clear` - Очистить корзину
- `POST /cart/calculate` - Рассчитать стоимость корзины

## Функции корзины

### Основные функции (cart.js)

#### `addToCartWithQuantity(productId)`
Добавляет товар в корзину или увеличивает его количество.

```javascript
window.addToCartWithQuantity('1'); // Добавить товар с ID '1'
```

**Параметры:**
- `productId` (string) - ID товара

**Возвращает:** ничего

**Особенности:**
- Автоматическая миграция старых данных корзины
- Проверка на некорректные ID
- Автоматическое обновление счетчика в UI

#### `getCartWithQuantity()`
Получает корзину из localStorage.

```javascript
const cart = window.getCartWithQuantity();
// Возвращает объект: { "1": 2, "5": 1 } - товар ID "1" в количестве 2, товар ID "5" в количестве 1
```

**Возвращает:** объект `{ productId: quantity }`

#### `saveCartWithQuantity(cartWithQuantity)`
Сохраняет корзину в localStorage.

```javascript
const cart = { "1": 2, "5": 1 };
window.saveCartWithQuantity(cart);
```

#### `removeFromCart(productId)`
Удаляет товар из корзины или уменьшает его количество.

```javascript
window.removeFromCart('1');
```

#### `clearCart()`
Полностью очищает корзину.

```javascript
window.clearCart();
```

#### `updateCartCount()`
Обновляет счетчик корзины в хедере страницы.

```javascript
window.updateCartCount();
```

### Функции каталога (catalog.js)

#### `addToCart(bookId)`
Добавляет пластинку в корзину из каталога.

```javascript
addToCart('1'); // Добавить пластинку с ID '1'
```

**Особенности:**
- Автоматически находит пластинку в массиве товаров
- Использует `window.addToCartWithQuantity()` для добавления
- Показывает уведомление пользователю
- Обновляет счетчик корзины

#### `loadCartCount()`
Загружает и отображает количество товаров в корзине.

```javascript
loadCartCount();
```

### Функции страницы детализации (book-detail.js)

#### `addToCartFromDetail(bookId, bookData)`
Добавляет пластинку в корзину со страницы детализации.

```javascript
addToCartFromDetail('1', {
    id: '1',
    name: 'Название пластинки',
    price: 199,
    cover_url: 'url'
});
```

**Параметры:**
- `bookId` (string) - ID пластинки
- `bookData` (object) - Данные пластинки

#### `updateCartCount()`
Обновляет счетчик корзины на странице детализации.

#### `showNotification(message)`
Показывает уведомление о добавлении товара.

## Использование

### Добавление товара из каталога

1. Пользователь нажимает кнопку "В корзину" на карточке товара
2. Вызывается `addToCart(bookId)`
3. Товар добавляется через `addToCartWithQuantity()`
4. Счетчик корзины обновляется
5. Показывается уведомление

### Добавление товара со страницы детализации

1. Пользователь нажимает кнопку "В корзину" на странице детализации
2. Вызывается `addToCartFromDetail(bookId, bookData)`
3. Товар добавляется через `addToCartWithQuantity()`
4. Счетчик корзины обновляется
5. Показывается уведомление

### Структура данных корзины

Корзина хранится в localStorage под ключом `cartWithQuantity` в формате:

```json
{
  "1": 2,
  "5": 1,
  "12": 3
}
```

Где ключ - это ID товара (строка), значение - количество товара (число).

## Интеграция с сервером

### Синхронизация корзины

Корзина автоматически синхронизируется с сервером при:
- Добавлении товара
- Удалении товара
- Очистке корзины
- Загрузке страницы корзины

### Обработка ошибок

При ошибках синхронизации:
- Корзина продолжает работать локально
- Ошибка логируется в консоль
- Пользователь уведомляется о проблемах через UI

## Тестирование

### Автоматические тесты

Файл `test_cart_functionality_full.html` содержит комплексные тесты:

1. **Тесты системы корзины** - проверка базовых функций
2. **Тесты каталога** - проверка интеграции с каталогом
3. **Тесты страницы детализации** - проверка интеграции со страницей детализации
4. **Интеграционные тесты** - проверка взаимодействия компонентов

### Запуск тестов

```bash
# Открыть файл тестов в браузере
start test_cart_functionality_full.html
```

Или просто откройте `test_cart_functionality_full.html` в браузере.

### Ручное тестирование

В тестовом файле доступны кнопки для:
- Добавления товаров вручную
- Просмотра состояния корзины
- Очистки корзины

## Миграция данных

Система автоматически мигрирует старые данные корзины из формата массива в новый формат объекта:

**Старый формат:**
```json
[
  { "id": "1", "quantity": 2 },
  { "id": "5", "quantity": 1 }
]
```

**Новый формат:**
```json
{
  "1": 2,
  "5": 1
}
```

Миграция происходит автоматически при первом использовании новой системы.

## Известные проблемы и решения

### Проблема: Товар не добавляется в корзину

**Решение:**
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что `cart.js` загружен до `catalog.js`
3. Проверьте, что ID товара корректен

### Проблема: Счетчик корзины не обновляется

**Решение:**
1. Убедитесь, что элемент с ID `cart-count` существует в DOM
2. Проверьте, что функция `updateCartCount()` вызывается
3. Обновите страницу

### Проблема: Корзина не синхронизируется с сервером

**Решение:**
1. Проверьте, что сервис корзины запущен (порт 8002)
2. Проверьте сетевые запросы в DevTools
3. Убедитесь, что пользователь авторизован (если требуется)

## Разработка

### Добавление новой функциональности

1. Все функции корзины должны быть экспортированы в `window`
2. Используйте `console.log()` для отладки
3. Обновляйте тесты при добавлении новых функций

### Стили уведомлений

Уведомления используют следующие стили:
- Фон: `#FF9900`
- Текст: `#0F1014`
- Анимация: slideIn/slideOut
- Длительность показа: 3 секунды

## Связанные файлы

- `src/scripts/cart.js` - Основной модуль корзины
- `src/scripts/catalog.js` - Интеграция с каталогом
- `src/scripts/book-detail.js` - Интеграция со страницей детализации
- `src/cart.html` - Страница корзины
- `test_cart_functionality_full.html` - Тесты функциональности
- `TEST_REPORT_CART.md` - Отчет о тестировании

## Версия

Документация актуальна на: Декабрь 2024

## Автор

Документация создана как часть проекта audio-store.

