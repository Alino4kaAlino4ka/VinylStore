# Руководство по миграции на MySQL

## 📋 Обзор

Этот документ описывает процесс миграции проекта с SQLite на MySQL, включая настройку базы данных, миграцию данных и тестирование.

---

## 🎯 Цели миграции

- Переход на production-ready базу данных MySQL
- Улучшение производительности и масштабируемости
- Поддержка конкурентных подключений
- Сохранение всех данных из SQLite

---

## 📦 Предварительные требования

1. **MySQL установлен** на вашем компьютере
2. **MySQL сервис запущен** (проверьте через `services.msc` или `mysql -u root -p`)
3. **Python пакет pymysql установлен:**
   ```bash
   pip install pymysql
   ```
   (уже включен в `requirements.txt`)

---

## 🚀 Быстрая настройка MySQL

### Автоматическая настройка (рекомендуется)

```bash
python setup_mysql_auto.py
```

Этот скрипт автоматически:
- ✅ Создаст базу данных `audio_store`
- ✅ Создаст пользователя `user` с паролем `1234`
- ✅ Выдаст все необходимые права
- ✅ Проверит подключение

**Требуется:** Доступ к MySQL как root (потребуется ввести пароль root)

### Ручная настройка

1. **Выполните SQL скрипт:**
   ```bash
   mysql -u root -p < database/create_mysql_database.sql
   ```

2. **Или вручную через MySQL клиент:**
   ```sql
   CREATE DATABASE audio_store CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   CREATE USER 'user'@'localhost' IDENTIFIED BY '1234';
   GRANT ALL PRIVILEGES ON audio_store.* TO 'user'@'localhost';
   FLUSH PRIVILEGES;
   ```

---

## ⚙️ Конфигурация

### Обновление config.env

Убедитесь, что в `config.env` указана правильная строка подключения:

```env
# Для MySQL (production):
DATABASE_URL=mysql+pymysql://user:1234@localhost/audio_store

# Для SQLite (development) - закомментировано:
# DATABASE_URL=sqlite:///./audio_store.db
```

### Формат строки подключения

```
mysql+pymysql://USER:PASSWORD@HOST:PORT/DATABASE_NAME
```

- `USER`: имя пользователя MySQL (по умолчанию: `user`)
- `PASSWORD`: пароль пользователя (по умолчанию: `1234`)
- `HOST`: хост MySQL сервера (по умолчанию: `localhost`)
- `PORT`: порт MySQL (по умолчанию: `3306`, можно не указывать)
- `DATABASE_NAME`: имя базы данных (по умолчанию: `audio_store`)

---

## 🗄️ Инициализация базы данных

### Создание таблиц

```bash
python init_db.py
```

Этот скрипт:
- ✅ Определяет тип БД (MySQL/SQLite) из `config.env`
- ✅ Создает все необходимые таблицы
- ✅ Выводит информативные сообщения о процессе

**Результат:** Все таблицы созданы в базе данных `audio_store`

### Заполнение начальными данными

```bash
python seed_db.py
```

Этот скрипт:
- ✅ Заполняет базу данных тестовыми данными
- ✅ Проверяет наличие существующих данных (избегает дубликатов)
- ✅ Выводит статистику после заполнения

---

## 📦 Миграция данных из SQLite

### Автоматическая миграция

```bash
python migrate_sqlite_to_mysql.py
```

Этот скрипт автоматически:
- ✅ Находит все SQLite файлы в проекте:
  - `audio_store.db` (корневой)
  - `services/auth/audio_store.db`
  - `services/prompts-manager/audio_store.db`
  - `services/catalog/audio_store.db`
- ✅ Извлекает данные из каждой SQLite базы
- ✅ Переносит данные в MySQL с сохранением связей
- ✅ Выводит статистику миграции

**Порядок миграции:**
1. Артисты (Artists)
2. Категории (Categories)
3. Виниловые пластинки (VinylRecords) - с сохранением связей
4. Пользователи (Users)
5. Промпты (Prompts)
6. Заказы (Orders)
7. Элементы заказов (OrderItems)

**Примечания:**
- Скрипт проверяет наличие данных перед миграцией
- Избегает дубликатов (unique constraints)
- Сохраняет все связи между таблицами

---

## 🧪 Тестирование

### Запуск всех тестов базы данных

```bash
python tests/run_all_database_tests.py
```

### Запуск отдельных тестов

```bash
# Тесты подключения к MySQL (8 тестов)
python tests/test_mysql_connection.py

# Тесты операций с БД (5 тестов)
python tests/test_database_operations.py
```

### Диагностика проблем

```bash
# Проверка настроек MySQL
python fix_mysql_setup.py
```

Этот скрипт:
- ✅ Проверяет доступность MySQL сервера
- ✅ Проверяет существование пользователя
- ✅ Проверяет существование базы данных
- ✅ Проверяет права пользователя
- ✅ Предлагает SQL команды для исправления проблем

---

## 📊 Результаты тестирования

### Статус тестов: ✅ ВСЕ ПРОЙДЕНЫ

| Категория | Тестов | Пройдено |
|-----------|--------|----------|
| Подключение к MySQL | 8 | 8 ✅ |
| Операции с БД | 5 | 5 ✅ |
| **ИТОГО** | **13** | **13 ✅** |

**Подробные результаты:** См. [`tests/FINAL_TEST_REPORT.md`](../tests/FINAL_TEST_REPORT.md)

---

## 🔧 Обновленные компоненты

### Модели базы данных (`database/models.py`)

Все String поля обновлены для MySQL (добавлены длины):

```python
# Было (для SQLite):
name = Column(String, nullable=False)

# Стало (для MySQL):
name = Column(String(255), nullable=False)
```

**Обновленные поля:**
- `Artist.name`: String(255)
- `Category.name`: String(255)
- `VinylRecord.title`: String(255)
- `VinylRecord.cover_image_url`: String(500)
- `User.email`: String(255)
- `User.hashed_password`: String(255)
- `Prompt.id`: String(255)
- `Prompt.name`: String(255)

### Подключение к БД (`database/connection.py`)

- ✅ Автоматическое определение типа БД (SQLite/MySQL)
- ✅ Правильная обработка `connect_args` (только для SQLite)
- ✅ Поддержка MySQL через `pymysql`

### Сервисы

Все сервисы автоматически используют MySQL из `config.env`:
- ✅ `services/catalog/main.py`
- ✅ `services/auth/main.py`
- ✅ `services/orders/main.py`
- ✅ `services/users/main.py`
- ✅ `services/prompts-manager/main.py`

---

## 📁 Структура файлов

```
├── database/
│   ├── connection.py              # Подключение к БД (обновлено для MySQL)
│   ├── models.py                  # Модели (обновлены для MySQL)
│   ├── create_mysql_database.sql  # SQL скрипт создания БД
│   └── MYSQL_SETUP.md             # Инструкция по настройке
├── init_db.py                     # Инициализация таблиц
├── seed_db.py                     # Заполнение начальными данными
├── migrate_sqlite_to_mysql.py     # Миграция данных
├── setup_mysql_auto.py            # Автоматическая настройка MySQL
├── fix_mysql_setup.py             # Диагностика проблем
└── tests/
    ├── test_mysql_connection.py   # Тесты подключения
    ├── test_database_operations.py # Тесты операций
    ├── run_all_database_tests.py  # Запуск всех тестов
    ├── TEST_CHECKLIST.md          # Чек-лист тестов
    └── FINAL_TEST_REPORT.md       # Финальный отчет
```

---

## 🛠️ Устранение проблем

### Ошибка: Access denied for user 'user'@'localhost'

**Решение:**
```bash
python setup_mysql_auto.py
```

Или вручную:
```sql
CREATE USER 'user'@'localhost' IDENTIFIED BY '1234';
GRANT ALL PRIVILEGES ON audio_store.* TO 'user'@'localhost';
FLUSH PRIVILEGES;
```

### Ошибка: Unknown database 'audio_store'

**Решение:**
```sql
CREATE DATABASE audio_store CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### Ошибка: VARCHAR requires a length on dialect mysql

**Решение:** Уже исправлено в `database/models.py`. Если видите эту ошибку, убедитесь, что используете последнюю версию моделей.

### Ошибка: Can't connect to MySQL server

**Проверьте:**
1. MySQL сервис запущен: `services.msc` → MySQL
2. Порт 3306 доступен
3. Файрвол не блокирует подключение

---

## 📝 Чек-лист миграции

- [ ] MySQL установлен и запущен
- [ ] База данных создана (`python setup_mysql_auto.py`)
- [ ] `config.env` обновлен с MySQL строкой подключения
- [ ] Таблицы инициализированы (`python init_db.py`)
- [ ] Данные мигрированы из SQLite (`python migrate_sqlite_to_mysql.py`)
- [ ] Все тесты пройдены (`python tests/run_all_database_tests.py`)
- [ ] Сервисы запускаются и подключаются к MySQL

---

## 🎯 Следующие шаги

После успешной миграции:

1. ✅ База данных готова к использованию
2. ✅ Все сервисы могут подключаться к MySQL
3. ✅ Данные успешно мигрированы
4. ✅ Все тесты пройдены

**Готово к использованию в production!** 🎉

---

## 📚 Дополнительная документация

- [Настройка MySQL](database/MYSQL_SETUP.md) - подробная инструкция
- [Чек-лист тестов](tests/TEST_CHECKLIST.md) - полный список тестов
- [Финальный отчет](tests/FINAL_TEST_REPORT.md) - результаты тестирования

