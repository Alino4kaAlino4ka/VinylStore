# 💬 Документация: AI-виртуальный консультант (чат-бот)

## Обзор

AI-виртуальный консультант — это интерактивный чат-бот, который помогает пользователям выбирать виниловые пластинки через живой диалог. Консультант использует искусственный интеллект для анализа предпочтений пользователя и предоставления персонализированных рекомендаций.

## 🎯 Основные возможности

- **Диалоговый интерфейс**: Естественное общение с пользователем через текстовый чат
- **Персонализированные рекомендации**: Анализ предпочтений и подбор подходящих пластинок
- **Сравнение пластинок**: Объяснение различий между разными вариантами
- **Поиск похожих пластинок**: Поиск пластинок по стилю, настроению или исполнителю
- **Контекстная помощь**: Учет текущей страницы пользователя (если на странице детализации)
- **История диалога**: Сохранение истории разговора для контекста

## 🏗️ Архитектура

### Компоненты системы

```
┌─────────────┐
│  Frontend   │
│  (chat.js)  │
└──────┬──────┘
       │ HTTP POST
       │
       ▼
┌─────────────────┐
│ Recommender      │
│ Service          │
│ (Port 8004)      │
└──────┬──────────┘
       │
       ├──► Prompts Manager (GET prompt)
       │
       ├──► Catalog API (GET products)
       │
       └──► OpenRouter API (LLM call)
```

### Бэкенд (Recommender Service)

**Эндпоинт**: `POST /api/v1/chat/message`

**Модели данных**:
- `ChatMessage`: Сообщение пользователя или ассистента
- `ChatRequest`: Запрос с сообщением и историей диалога
- `ChatResponse`: Ответ консультанта

**Процесс обработки**:
1. Получение базового промпта из prompts-manager (`chat_consultant_prompt`)
2. Получение каталога пластинок из catalog API
3. Формирование системного промпта с контекстом
4. Добавление истории диалога (последние 10 сообщений)
5. Вызов LLM через OpenRouter
6. Очистка markdown из ответа
7. Возврат ответа пользователю

### Headless AI (Prompts Manager)

**Промпт**: `chat_consultant_prompt`

Промпт определяет:
- Роль консультанта (эксперт по виниловым пластинкам)
- Стиль общения (дружелюбный, профессиональный)
- Возможности (рекомендации, сравнение, поиск)
- Ограничения (только информация из каталога)
- Формат ответов (естественный диалог, эмодзи)

Промпт можно редактировать через админ-панель (Центр управления AI).

### Фронтенд

**Модуль**: `src/scripts/chat.js`

**Компоненты UI**:
- Плавающая кнопка чата (правый нижний угол)
- Окно чата с историей сообщений
- Поле ввода с кнопкой отправки
- Индикатор печати при генерации ответа
- Кнопки очистки и закрытия чата

**Хранение данных**:
- История диалога сохраняется в `localStorage` (ключ: `vinyl_shop_chat_history`)
- Ограничение: последние 20 сообщений
- История сохраняется автоматически при каждом сообщении

## 📋 Использование

### Для пользователей

1. **Открытие чата**: Нажмите на кнопку чата в правом нижнем углу страницы
2. **Отправка сообщения**: Введите вопрос в поле ввода и нажмите "Отправить" или Enter
3. **Примеры вопросов**:
   - "Какие у вас есть пластинки рока?"
   - "Посоветуй что-то похожее на The Beatles"
   - "Что лучше для романтического вечера?"
   - "Чем отличается пластинка #5 от пластинки #10?"
4. **Очистка истории**: Нажмите кнопку очистки (🗑️) в заголовке чата

### Для администраторов

**Редактирование промпта консультанта**:
1. Откройте админ-панель
2. Перейдите в "Центр управления AI"
3. Найдите промпт `chat_consultant_prompt`
4. Отредактируйте текст промпта
5. Сохраните изменения

**Сброс промпта к дефолтному**:
1. В админ-панели найдите промпт `chat_consultant_prompt`
2. Нажмите кнопку "Сбросить к дефолту"

## 🔧 Технические детали

### API эндпоинт

**URL**: `http://127.0.0.1:8004/api/v1/chat/message`

**Метод**: `POST`

**Request Body**:
```json
{
  "message": "Какие у вас есть пластинки рока?",
  "history": [
    {"role": "user", "content": "Привет"},
    {"role": "assistant", "content": "Привет! Чем могу помочь?"}
  ],
  "current_product_id": 5,
  "model": "gpt-4"
}
```

**Response**:
```json
{
  "response": "У нас есть отличная подборка рок-пластинок! Например, пластинка #2 - Pink Floyd с их культовым альбомом...",
  "success": true
}
```

### Обработка контекста

**Контекст каталога**:
- При каждом запросе получается актуальный каталог из catalog API
- В системный промпт передается компактный список пластинок (ID, название, исполнитель, цена, описание)

**Контекст текущей страницы**:
- Если пользователь на странице детализации (`book-detail.html?id=5`), информация о текущей пластинке передается в контекст
- Консультант может учитывать это при ответах

**История диалога**:
- Используются последние 10 сообщений для контекста
- История передается в формате `[{"role": "user|assistant", "content": "..."}]`

### Очистка markdown

Ответы консультанта автоматически очищаются от markdown-символов:
- `**жирный текст**` → `жирный текст`
- `*курсив*` → `курсив`
- `# заголовки` → удаляются
- Списки и нумерация → удаляются
- Специальные кавычки → заменяются на обычные

Это обеспечивает чистый и читаемый текст в интерфейсе.

## 🎨 UI/UX

### Дизайн виджета

- **Позиция**: Правый нижний угол страницы (фиксированная позиция)
- **Кнопка открытия**: Круглая кнопка с иконкой 💬
- **Окно чата**: Темная тема, соответствующая общему дизайну сайта
- **Сообщения пользователя**: Оранжевый градиент (справа)
- **Сообщения консультанта**: Темно-серая тема (слева)
- **Подсветка ID пластинок**: Оранжевая подсветка упоминаний ID (например: #5)

### Адаптивность

- На мобильных устройствах окно чата занимает почти весь экран
- Кнопка чата уменьшается на мобильных
- Все элементы адаптируются под размер экрана

## 🐛 Обработка ошибок

### Типы ошибок

1. **Сервис недоступен**:
   - Показывается сообщение: "Извините, произошла ошибка: [детали]. Пожалуйста, попробуйте позже."

2. **Таймаут LLM**:
   - Показывается сообщение об ошибке с предложением попробовать позже

3. **Ошибка подключения**:
   - Проверяется доступность сервисов (catalog, recommender, prompts-manager)
   - Показывается понятное сообщение об ошибке

### Логирование

Все ошибки логируются в консоль браузера для отладки:
```javascript
console.error('Ошибка при отправке сообщения:', error);
```

## 📊 Интеграция с другими функциями

### С AI-рекомендациями

- Консультант может рекомендовать пластинки, используя те же алгоритмы
- Можно использовать консультанта как альтернативу форме рекомендаций

### С каталогом

- Консультант всегда работает с актуальным каталогом
- При добавлении новых пластинок они автоматически становятся доступными консультанту

### С админ-панелью

- Промпт консультанта можно редактировать через админ-панель
- Не требуется перезапуск сервисов для применения изменений

## 🧪 Тестирование

### API тесты

**Файл**: `tests/test_chat_api.py`

**Покрытие**: 8 тестов

**Что тестируется**:
- ✅ Наличие промпта `chat_consultant_prompt` в prompts-manager
- ✅ Существование и доступность эндпоинта `/api/v1/chat/message`
- ✅ Формат ответа API (структура данных)
- ✅ Работа чата с историей диалога
- ✅ Работа с контекстом текущей пластинки (`current_product_id`)
- ✅ Очистка markdown из ответов
- ✅ Валидация входящих запросов (пустые сообщения, неверный формат)
- ✅ Обработка ошибок (несуществующий product_id, ошибки сервисов)

**Запуск тестов**:
```bash
# Отдельный запуск
python tests/test_chat_api.py

# Через bat файл
run_chat_tests.bat

# В составе всех тестов
python run_all_tests.py
```

**Подробная документация по тестированию**: 
- [TEST_CHAT_README.md](../tests/TEST_CHAT_README.md) - руководство по тестированию
- [CHAT_TESTING_REPORT.md](CHAT_TESTING_REPORT.md) - отчет о тестировании

### Фронтенд тесты

**Файл**: `tests/test_chat_frontend.html`

**Покрытие**: 9 тестов

**Что тестируется**:
- ✅ Наличие всех элементов интерфейса
- ✅ Инициализация модуля чата
- ✅ Отправка сообщений к API
- ✅ Сохранение истории в localStorage
- ✅ Очистка истории
- ✅ Доступность API сервисов
- ✅ Запросы к API
- ✅ Обработка ошибок API
- ✅ UI/UX элементы (открытие/закрытие, индикаторы, форматирование)

**Запуск тестов**:
Откройте `tests/test_chat_frontend.html` в браузере

### Ручное тестирование

1. Откройте главную страницу
2. Нажмите на кнопку чата
3. Отправьте несколько сообщений
4. Проверьте сохранение истории (перезагрузите страницу)
5. Проверьте очистку истории
6. Проверьте работу на странице детализации пластинки

### Предварительные требования для тестов

Перед запуском тестов убедитесь:
1. Все сервисы запущены (`python start_services_final.py`)
2. API ключ установлен в `config.env`
3. В каталоге есть товары

## 🔒 Безопасность

### Ограничения

- Консультант использует только информацию из каталога
- Не выдумывает пластинки, которых нет в каталоге
- Все данные передаются через HTTPS (в production)

### Хранение данных

- История диалога хранится только в localStorage браузера
- Не передается на сервер (кроме текущего запроса)
- Может быть очищена пользователем в любой момент

## 📝 Промпт консультанта

### Структура промпта

Промпт состоит из нескольких разделов:

1. **Роль**: Определение роли консультанта
2. **Стиль общения**: Как консультант должен общаться
3. **Важные правила**: Что можно и нельзя делать
4. **Формат ответов**: Как структурировать ответы
5. **Контекст**: Какая информация доступна

### Редактирование промпта

Промпт можно редактировать через админ-панель:
1. Админ-панель → Центр управления AI
2. Найти `chat_consultant_prompt`
3. Отредактировать текст
4. Сохранить

Изменения применяются немедленно без перезапуска сервисов.

## 🚀 Производительность

### Оптимизации

- История диалога ограничена последними 10 сообщениями для LLM
- Каталог передается в компактном формате
- Используется асинхронная обработка запросов
- Кэширование не используется (всегда актуальный каталог)

### Время ответа

- Типичное время ответа: 3-10 секунд
- Зависит от нагрузки на OpenRouter API
- При таймауте показывается ошибка

## 📚 Дополнительные ресурсы

- [Обзор AI-функций](AI_FEATURES_OVERVIEW.md)
- [Документация Prompts Manager](PROMPTS_MANAGER_FULL_DOCUMENTATION.md)
- [Документация Recommender Service](RECOMMENDER_SERVICE_REPORT.md)
- [Конфигурация портов](../PORTS_CONFIG.md)
- [Тестирование чата](CHAT_TESTING_REPORT.md)

## 🔄 История изменений

### Версия 1.0 (Текущая)
- ✅ Базовый функционал чата
- ✅ Интеграция с prompts-manager
- ✅ Сохранение истории в localStorage
- ✅ Очистка markdown из ответов
- ✅ Контекстная помощь на странице детализации
- ✅ Подсветка ID пластинок в ответах

## 💡 Рекомендации по использованию

### Для лучших результатов

1. **Задавайте конкретные вопросы**: "Какие пластинки рока у вас есть?" лучше, чем "Что посоветуешь?"
2. **Указывайте предпочтения**: "Люблю джаз и классику" помогает консультанту лучше подобрать рекомендации
3. **Используйте ID пластинок**: Консультант указывает ID в ответах - используйте их для навигации
4. **Задавайте уточняющие вопросы**: Консультант может задавать вопросы - отвечайте на них для лучших рекомендаций

### Типичные сценарии

**Сценарий 1: Поиск по жанру**
```
Пользователь: Какие у вас есть пластинки джаза?
Консультант: [Рекомендует джазовые пластинки с ID]
```

**Сценарий 2: Поиск похожих**
```
Пользователь: Что-то похожее на The Beatles
Консультант: [Рекомендует похожие пластинки с объяснением]
```

**Сценарий 3: Сравнение**
```
Пользователь: Чем отличается пластинка #5 от #10?
Консультант: [Объясняет различия]
```

## ❓ FAQ

**В: Почему консультант не отвечает?**
О: Проверьте, что сервисы запущены (catalog, recommender, prompts-manager). Проверьте консоль браузера на наличие ошибок.

**В: Можно ли очистить историю?**
О: Да, нажмите кнопку очистки (🗑️) в заголовке чата.

**В: История сохраняется между сеансами?**
О: Да, история сохраняется в localStorage и загружается при следующем открытии чата.

**В: Как изменить стиль общения консультанта?**
О: Отредактируйте промпт `chat_consultant_prompt` в админ-панели.

**В: Консультант выдумывает пластинки?**
О: Нет, консультант использует только информацию из каталога. Если пластинка не найдена, он сообщит об этом.

