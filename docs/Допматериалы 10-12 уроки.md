## Практические уроки: Промпты и дополнительные материалы (Уроки 10-12)

### **Урок 10: Архитектура Headless AI. Платформа управления промптами**

#### 🤖 **Промпт для Cursor AI (Проектирование БД):**
```
Добавь в этот файл новую SQLAlchemy-модель `Prompt`.
Она должна включать поля:
- `id`: первичный ключ, строковый, не автоинкрементный (мы будем задавать ID сами, например 'recommendation_prompt').
- `name`: человекочитаемое название (String).
- `template`: сам текст промпта (Text, для длинных текстов).
```

#### 🤖 **Промпт для Cursor AI (Разработка сервиса `prompts-manager`):**
```
Задача: Создай FastAPI-приложение для управления промптами, хранящимися в базе данных.

**Архитектура:**
-   Подключись к той же базе данных SQLite, что и другие сервисы (`database/database.py`).
-   Используй SQLAlchemy-модель `Prompt` из `database/models.py`.
-   Создай Pydantic-схему `PromptSchema` для валидации данных API.

**Логика при запуске:**
-   Напиши функцию, которая при старте приложения проверяет, существуют ли в БД промпты с ID 'recommendation_prompt' и 'description_prompt'. Если нет, она должна их создать с текстами по умолчанию.

**API Эндпоинты (RESTful):**
-   `GET /api/v1/prompts`: возвращает список всех промптов.
-   `GET /api/v1/prompts/{prompt_id}`: возвращает один промпт по ID.
-   `PUT /api/v1/prompts/{prompt_id}`: обновляет `template` промпта в базе данных.
```

#### 🤖 **Промпт для Cursor AI (Рефакторинг `recommender`):**
```
Задача: Проведи рефакторинг этого эндпоинта. Он больше не должен использовать захардкоженный промпт.

**Логика:**
1.  В самом начале функции сделай асинхронный GET-запрос к нашему новому сервису: `http://localhost:8004/api/v1/prompts/recommendation_prompt`.
2.  Из полученного JSON-ответа извлеки значение поля `template`.
3.  Используй этот динамически полученный шаблон для дальнейшей работы.
```

#### 🤖 **Промпт для Cursor AI (Frontend: Редактор промптов и кнопка генерации):**
```
Задача: Создай интерфейс для редактора промптов и добавь кнопку для генерации описания товара.

**1. HTML (`admin.html`):**
   - Добавь заголовок `<h2>Центр управления AI (Редактор промптов)</h2>`.
   - Создай пустой контейнер `<div id="prompts-list"></div>`.

**2. CSS (в `admin.css` или в теге `<style>`):**
   - Опиши стили для карточки `.prompt-card` и `textarea` внутри.

**3. JavaScript (`admin.js`):**
   - **Редактор промптов:**
     - Напиши функцию `fetchAndRenderPrompts`, которая делает GET-запрос на `/api/v1/prompts` и для каждого промпта создает карточку с `textarea` и кнопкой "Сохранить".
     - Добавь логику сохранения: при клике на "Сохранить" отправлять PUT-запрос с новым текстом промпта.
   - **Кнопка генерации описания:**
     - Модифицируй функцию `fetchAndRenderProducts` (из Урока 7): в цикле создания строк таблицы для каждого товара добавляй кнопку `<button class="generate-desc-btn">AI Описание</button>` с `data-id` товара.
     - В общем обработчике кликов на таблицу добавь логику для `.generate-desc-btn`: при клике делать POST-запрос на `http://localhost:8003/generate-description` (порт `recommender`), передавая ID и название книги. Полученный ответ показывать в `alert()` и обновлять список товаров.
```

---

### **Урок 11: Тестирование, отладка и подготовка к развертыванию**

#### 🤖 **Промпт для Cursor AI (Чек-лист для тестирования):**
```
Задача: Создай чек-лист в формате Markdown для ручного тестирования нашего веб-приложения "Audio Store".

**Структура чек-листа:**

1.  **Сценарии для пользователя (Клиентская часть):**
    -   Регистрация нового пользователя.
    -   Вход и выход из системы.
    -   Просмотр каталога.
    -   Добавление товара в корзину (из каталога).
    -   Оформление заказа.
    -   Проверка, что после заказа корзина очищается.

2.  **Сценарии для администратора (Admin-панель):**
    -   Добавление новой книги.
    -   Редактирование книги.
    -   Удаление книги.
    -   Проверка, что изменения видны в общем каталоге.

3.  **Тестирование AI-функций (Самое важное):**
    -   **Кейс 1 (Успех):** Запрос корректной рекомендации.
    -   **Кейс 2 (Успех):** Генерация описания для книги.
    -   **Кейс 3 (Стресс-тест):** Что произойдет, если в поле рекомендации ввести пустой запрос или бессмыслицу?
    -   **Кейс 4 (Стресс-тест):** Что произойдет, если намеренно указать неверный API-ключ для OpenRouter? Приложение должно не упасть, а выдать ошибку.
```

#### 🤖 **Промпт для Cursor AI (Повышение надежности эндпоинта):**
```
Задача: Сделай этот эндпоинт более надежным.

**Требования к рефакторингу:**
1.  Оберни блок кода, который делает запрос к OpenRouter и парсит JSON, в конструкцию `try...except`.
2.  **Обработка ошибок API:** Отлавливай исключения `httpx.HTTPStatusError`. Если такая ошибка произошла, верни клиенту `HTTPException` со статусом 502 (Bad Gateway) и сообщением "Ошибка при обращении к внешнему AI сервису".
3.  **Обработка ошибок парсинга:** Отлавливай исключения, связанные с некорректным JSON (`json.JSONDecodeError` или общее `Exception`). Если ответ от LLM — это невалидный JSON, верни `HTTPException` со статусом 500 и сообщением "Не удалось обработать ответ от AI. Попробуйте изменить промпт.".
```

#### 🤖 **Промпт для Cursor AI (Создание `requirements.txt`):**
```
Проанализируй все `.py` файлы в папке `services`. Собери полный список всех уникальных внешних зависимостей (fastapi, pydantic, httpx, uvicorn и т.д.) и создай в корне проекта единый файл `requirements.txt`.
```

#### 🤖 **Промпт для Cursor AI (Использование переменных окружения):**
```
Задача: Замени эту жестко закодированную строку на чтение ключа из переменной окружения.

**Требования:**
1.  Импортируй модуль `os`.
2.  Получай ключ с помощью `os.getenv('OPENROUTER_API_KEY')`.
3.  Добавь проверку: если переменная окружения не установлена, приложение должно падать при старте с информативной ошибкой, например, `raise ValueError('Необходимо установить переменную окружения OPENROUTER_API_KEY')`.
```

---

### **Урок 12: Гибкость архитектуры и деплой**

#### 🤖 **Промпт для Cursor AI (Объединение микросервисов в монолит):**
```
Задача: Объедини все микросервисы из папки `services` в одно FastAPI-приложение в файле `main_app.py`.

**План действий:**
1.  **Инициализация:** В `main_app.py` создай основной экземпляр `app = FastAPI()`. Настрой CORS и раздачу статики из папки `src`.
2.  **База данных:** Импортируй `init_db` из `database.database` и вызови эту функцию при старте приложения через `@app.on_event("startup")`, чтобы создать все таблицы.
3.  **Рефакторинг сервисов:**
    -   **Для каждого `.py` файла в папке `services`** (кроме `__init__.py`):
        -   Преобразуй `app = FastAPI()` в `router = APIRouter()`.
        -   Замени все декораторы `@app.*` на подходящие `@router.*`.
        -   Удали из каждого сервиса код, связанный с CORS и запуском `uvicorn`.
4.  **Сборка:**
    -   В `main_app.py` импортируй каждый `router` из модифицированных сервисов.
    -   Подключи каждый `router` к основному `app` с помощью `app.include_router()`, сохранив исходные префиксы URL.
5.  **Изменение межсервисных вызовов:**
    -   В файлах, где используется `httpx` для вызовов между сервисами (например, `recommender` или `orders`), замени HTTP-вызовы на **прямые асинхронные вызовы функций** из соответствующих модулей, так как весь код теперь в одном приложении.
```
