# 🧪 Тестирование редактора промптов

## Описание

Редактор промптов позволяет администраторам управлять AI-промптами через веб-интерфейс админ-панели. Это реализация **Шага 3** плана Headless AI архитектуры - "Центр управления AI".

## ✅ Функциональность

- ✅ Автоматическая загрузка всех промптов из prompts-manager
- ✅ Отображение промптов в виде карточек с редактируемыми textarea
- ✅ Сохранение изменений через PUT-запросы к API
- ✅ Визуальная индикация изменений (подсветка границы при изменении)
- ✅ Улучшенная обработка ошибок сети
- ✅ Валидация пустого контента
- ✅ Кнопка повторной попытки при ошибках загрузки

## 📋 Структура файлов

```
src/admin/
├── admin.html          # Главная страница админ-панели
└── admin.js            # JavaScript с функцией fetchAndRenderPrompts()

tests/
├── test_prompts_editor.html              # UI тест в браузере
└── test_prompts_editor_integration.py     # Python интеграционный тест
```

## 🚀 Быстрый старт

### 1. Запуск сервиса prompts-manager

```bash
# Вариант 1: Через основной скрипт (рекомендуется)
python start_services_final.py

# Вариант 2: Отдельный запуск
start_prompts_manager.bat

# Вариант 3: Вручную
cd services/prompts-manager
python -c "from main import app; import uvicorn; uvicorn.run(app, host='127.0.0.1', port=8007)"
```

### 2. Проверка доступности сервиса

```bash
curl http://127.0.0.1:8007/health
# Должен вернуть: {"status": "ok", "service": "prompts-manager"}
```

### 3. Тестирование

#### Тест 1: Python интеграционный тест

```bash
python test_prompts_editor_integration.py
```

**Ожидаемый результат:**
```
🧪 ИНТЕГРАЦИОННЫЙ ТЕСТ РЕДАКТОРА ПРОМПТОВ
✅ Сервис доступен!
✅ PASSED: Получено промптов: 2
✅ Найден recommendation_prompt
✅ Найден description_prompt
✅ Тест обновления: PASSED
✅ Оригинальный контент восстановлен
...
✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ!
```

#### Тест 2: UI тест в браузере

1. Откройте `test_prompts_editor.html` в браузере
2. Нажмите кнопки для проверки функциональности
3. Проверьте лог выполнения

**Проверяемые функции:**
- ✅ Health check API
- ✅ Загрузка промптов
- ✅ Обновление промпта
- ✅ Обработка ошибок

#### Тест 3: Реальная админ-панель

1. Убедитесь, что prompts-manager запущен (порт 8007)
2. Откройте `src/admin/admin.html` в браузере
3. Авторизуйтесь как администратор
4. Прокрутите до раздела **"Центр управления АІ (Редактор промптов)"**
5. Убедитесь, что промпты загрузились автоматически
6. Измените текст промпта в textarea
7. Нажмите "Сохранить"
8. Проверьте сообщение об успешном сохранении

## 📝 API Endpoints (prompts-manager)

### GET /api/v1/prompts
Возвращает список всех промптов.

**Запрос:**
```javascript
fetch('http://127.0.0.1:8007/api/v1/prompts')
```

**Ответ:**
```json
[
  {
    "id": 1,
    "name": "recommendation_prompt",
    "content": "Ты - эксперт по виниловым пластинкам...",
    "created_at": "2024-01-01T00:00:00",
    "updated_at": "2024-01-01T00:00:00"
  },
  {
    "id": 2,
    "name": "description_prompt",
    "content": "Ты - эксперт по написанию...",
    ...
  }
]
```

### PUT /api/v1/prompts/{prompt_name}
Обновляет содержимое промпта.

**Запрос:**
```javascript
fetch(`http://127.0.0.1:8007/api/v1/prompts/recommendation_prompt`, {
  method: 'PUT',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    content: 'Новое содержимое промпта'
  })
})
```

**Ответ:**
```json
{
  "id": 1,
  "name": "recommendation_prompt",
  "content": "Новое содержимое промпта",
  "created_at": "2024-01-01T00:00:00",
  "updated_at": "2024-01-01T12:00:00"
}
```

## 🔧 Отладка

### Проблема: Промпты не загружаются

**Причины:**
1. Сервис prompts-manager не запущен
2. Неверный порт (должен быть 8007)
3. Проблемы с CORS
4. Ошибки в консоли браузера

**Решение:**
1. Проверьте доступность: `curl http://127.0.0.1:8007/health`
2. Откройте консоль браузера (F12) и проверьте ошибки
3. Убедитесь, что сервис запущен на порту 8007

### Проблема: Ошибка при сохранении

**Причины:**
1. Пустой контент (валидация на фронтенде)
2. Сервер вернул ошибку (404, 422, 500)
3. Проблемы с сетью

**Решение:**
1. Проверьте, что textarea не пустой
2. Откройте консоль браузера для деталей ошибки
3. Проверьте логи сервиса prompts-manager

### Проблема: Изменения не сохраняются

**Причины:**
1. Промпт с таким именем не существует
2. Ошибка в базе данных
3. Проблемы с правами доступа

**Решение:**
1. Проверьте, что промпт существует: `GET /api/v1/prompts/{name}`
2. Проверьте логи сервиса
3. Проверьте базу данных `audio_store.db`

## ✅ Чек-лист тестирования

- [ ] Сервис prompts-manager запущен и доступен
- [ ] Health check проходит успешно
- [ ] GET /api/v1/prompts возвращает список промптов
- [ ] Промпты отображаются в админ-панели
- [ ] Textarea отображают корректный контент
- [ ] Изменения в textarea подсвечиваются оранжевой рамкой
- [ ] Кнопка "Сохранить" работает корректно
- [ ] Сообщение об успешном сохранении появляется
- [ ] PUT запрос обновляет промпт в базе данных
- [ ] Ошибки сети обрабатываются корректно
- [ ] Кнопка "Попробовать снова" работает при ошибках
- [ ] Валидация пустого контента работает
- [ ] Ошибки отображаются пользователю понятно

## 🎯 Результаты тестирования

После выполнения всех тестов вы должны увидеть:

1. **Python тест:** Все тесты пройдены (5/5)
2. **UI тест:** Все функции работают корректно
3. **Ручной тест:** Редактор работает в админ-панели

## 📚 Связанная документация

- [PROMPTS_MANAGER_FULL_DOCUMENTATION.md](./PROMPTS_MANAGER_FULL_DOCUMENTATION.md) - Полная документация prompts-manager
- [PROMPTS_MANAGER_TEST_REPORT.md](./PROMPTS_MANAGER_TEST_REPORT.md) - Отчет о тестировании API
- [HEADLESS_AI_IMPLEMENTATION_REPORT.md](./HEADLESS_AI_IMPLEMENTATION_REPORT.md) - Реализация Headless AI

## 🐛 Известные ограничения

1. **Нет создания новых промптов** - только редактирование существующих (можно добавить позже)
2. **Нет удаления промптов** - только редактирование (можно добавить позже)
3. **Нет версионирования** - изменения перезаписывают промпт (можно добавить позже)
4. **Нет предпросмотра** - изменения применяются сразу (можно добавить позже)

## 🔮 Планы на будущее

- [ ] Добавить возможность создания новых промптов
- [ ] Добавить удаление промптов
- [ ] Добавить историю изменений (версионирование)
- [ ] Добавить предпросмотр изменений перед сохранением
- [ ] Добавить экспорт/импорт промптов
- [ ] Добавить поиск по промптам
- [ ] Добавить теги/категории для промптов

