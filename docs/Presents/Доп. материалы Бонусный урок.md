## Практические уроки: Извлечение промптов и дополнительных материалов

### **Бонусный Урок: Миграция на MySQL для Production**

#### 💻 **SQL-запросы для настройки базы данных:**
```sql
-- Создаем новую базу данных для нашего проекта
CREATE DATABASE audio_store CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Создаем специального пользователя для нашего приложения
CREATE USER 'app_user'@'localhost' IDENTIFIED BY 'YourStrongPassword_123';

-- Выдаем этому пользователю полные права на управление нашей новой базой данных
GRANT ALL PRIVILEGES ON audio_store.* TO 'app_user'@'localhost';

-- Обновляем привилегии, чтобы изменения вступили в силу
FLUSH PRIVILEGES;
```

#### 🤖 **Промпт для AI-ассистента (Обновление подключения):**
```
Ты — DevOps-инженер. Модифицируй, пожалуйста, наш модуль `database/db.py`.

Задача: перенастроить подключение с SQLite на MySQL.

1.  **Измени строку подключения `SQLALCHEMY_DATABASE_URL`**:
    *   Новый формат: `mysql+pymysql://USER:PASSWORD@HOST/DB_NAME`
    *   Используй следующие данные:
        *   USER: `app_user`
        *   PASSWORD: `YourStrongPassword_123`
        *   HOST: `localhost`
        *   DB_NAME: `audio_store`
2.  **Удали `connect_args={"check_same_thread": False}`**: этот аргумент специфичен для SQLite и не нужен для MySQL.
```




