# 🗄️ Руководство по резервному копированию базы данных

## Обзор

Проект включает автоматизированный скрипт для создания резервных копий базы данных MySQL. Скрипт `scripts/utils/db/backup_db.py` позволяет создавать полные бэкапы базы данных с автоматическим управлением хранением старых копий.

## Основные возможности

- ✅ Автоматическое создание бэкапов с датой и временем
- ✅ Автоматическая очистка старых бэкапов (настраиваемый период хранения)
- ✅ Поддержка MySQL (через mysqldump)
- ✅ Кросс-платформенность (Windows, Linux, macOS)
- ✅ Настройка директории для сохранения бэкапов
- ✅ Детальная информация о процессе создания бэкапа

## Быстрый старт

### Создание бэкапа вручную

```bash
# Создать бэкап (сохраняется в project_root/backups/)
python scripts/utils/db/backup_db.py
```

### С указанием директории

```bash
# Указать свою директорию для бэкапов
python scripts/utils/db/backup_db.py --backup-dir /path/to/backups
```

### Настройка периода хранения

```bash
# Хранить бэкапы 60 дней (по умолчанию 30)
python scripts/utils/db/backup_db.py --keep-days 60

# Отключить автоматическую очистку старых бэкапов
python scripts/utils/db/backup_db.py --keep-days 0
```

## Использование

### Параметры командной строки

```bash
python scripts/utils/db/backup_db.py [OPTIONS]

Опции:
  --backup-dir DIR     Директория для сохранения бэкапов
                      (по умолчанию: project_root/backups/)
  
  --keep-days DAYS     Количество дней для хранения старых бэкапов
                      (по умолчанию: 30, 0 = отключить очистку)
  
  -h, --help          Показать справку
```

### Формат имени файла бэкапа

Бэкапы сохраняются с именами в формате:
```
audio_store_backup_YYYYMMDD_HHMMSS.sql
```

Пример: `audio_store_backup_20251105_143022.sql`

### Структура директории backups

```
backups/
├── audio_store_backup_20251101_020000.sql
├── audio_store_backup_20251102_020000.sql
├── audio_store_backup_20251103_020000.sql
└── ...
```

## Автоматизация

### Linux/macOS (cron)

#### Настройка ежедневного бэкапа в 2:00

1. Откройте crontab:
   ```bash
   crontab -e
   ```

2. Добавьте строку:
   ```bash
   0 2 * * * cd /path/to/project && python scripts/utils/db/backup_db.py
   ```

3. Сохраните и выйдите

#### Настройка с указанием директории

```bash
0 2 * * * cd /path/to/project && python scripts/utils/db/backup_db.py --backup-dir /var/backups/vinyl-store
```

#### Настройка с периодом хранения

```bash
0 2 * * * cd /path/to/project && python scripts/utils/db/backup_db.py --keep-days 60
```

### Windows (Task Scheduler)

1. Откройте **Планировщик заданий** (Task Scheduler)

2. Создайте новую задачу:
   - **Имя:** `Vinyl Store Database Backup`
   - **Триггер:** Ежедневно в 2:00
   - **Действие:** Запустить программу
   - **Программа:** `python`
   - **Аргументы:** `scripts/utils/db/backup_db.py`
   - **Рабочая папка:** `C:\path\to\project`

3. Сохраните задачу

#### Пример команды для Task Scheduler

```cmd
cd C:\path\to\project && python scripts/utils/db/backup_db.py --backup-dir C:\backups\vinyl-store
```

### Примеры автоматизации

#### Ежедневный бэкап с сохранением 30 дней
```bash
# Linux/macOS
0 2 * * * cd /opt/vinyl-store && python scripts/utils/db/backup_db.py

# Windows (в Task Scheduler)
cd C:\vinyl-store && python scripts/utils/db/backup_db.py
```

#### Еженедельный бэкап с сохранением 90 дней
```bash
# Linux/macOS (каждый понедельник в 3:00)
0 3 * * 1 cd /opt/vinyl-store && python scripts/utils/db/backup_db.py --keep-days 90

# Windows - настройте в Task Scheduler
```

#### Ежедневный бэкап в отдельную директорию
```bash
# Linux/macOS
0 2 * * * cd /opt/vinyl-store && python scripts/utils/db/backup_db.py --backup-dir /backups/database

# Windows
cd C:\vinyl-store && python scripts/utils/db/backup_db.py --backup-dir D:\backups\database
```

## Требования

### Обязательные

- **Python 3.9+**
- **MySQL Client Tools** (для mysqldump)
- **Доступ к базе данных** (указан в `config.env`)

### Проверка наличия mysqldump

```bash
# Linux/macOS
which mysqldump

# Windows
where mysqldump
```

Если mysqldump не найден, установите MySQL Client Tools:
- **Linux:** `sudo apt-get install mysql-client` (Debian/Ubuntu) или `sudo yum install mysql` (CentOS/RHEL)
- **macOS:** `brew install mysql-client`
- **Windows:** Скачайте с [MySQL Downloads](https://dev.mysql.com/downloads/mysql/)

## Конфигурация

Скрипт автоматически использует настройки из `config.env`:

```env
DATABASE_URL=mysql+pymysql://user:password@host/database_name
```

### Формат DATABASE_URL

Скрипт поддерживает следующие форматы:
- `mysql+pymysql://user:password@host/database`
- `mysql+pymysql://user:password@host:port/database`
- `mysql://user:password@host/database`

## Восстановление из бэкапа

### Восстановление всей базы данных

```bash
# Linux/macOS
mysql -u user -p audio_store < backups/audio_store_backup_20251105_143022.sql

# Windows
mysql -u user -p audio_store < backups\audio_store_backup_20251105_143022.sql
```

### Восстановление с указанием пароля

```bash
mysql -u user -p'password' audio_store < backups/audio_store_backup_20251105_143022.sql
```

### Восстановление конкретных таблиц

```bash
# Извлечь таблицу из бэкапа
grep -A 100 "CREATE TABLE.*artists" backups/audio_store_backup_20251105_143022.sql > artists.sql

# Восстановить таблицу
mysql -u user -p audio_store < artists.sql
```

## Мониторинг и логирование

### Проверка успешности бэкапа

Скрипт выводит информацию о процессе:
- ✅ Размер созданного бэкапа
- ✅ Количество удаленных старых бэкапов
- ❌ Ошибки (если есть)

### Логирование в файл

```bash
# Linux/macOS
0 2 * * * cd /path/to/project && python scripts/utils/db/backup_db.py >> /var/log/backup.log 2>&1

# Windows (в Task Scheduler добавьте в аргументы)
python scripts/utils/db/backup_db.py >> C:\logs\backup.log 2>&1
```

## Устранение неполадок

### Ошибка: "mysqldump не найден в PATH"

**Решение:**
1. Установите MySQL Client Tools (см. раздел "Требования")
2. Добавьте путь к mysqldump в PATH:
   - **Linux/macOS:** Добавьте в `~/.bashrc` или `~/.zshrc`
   - **Windows:** Добавьте в системные переменные окружения

### Ошибка: "Access denied for user"

**Решение:**
1. Проверьте правильность учетных данных в `config.env`
2. Убедитесь, что пользователь имеет права на чтение базы данных:
   ```sql
   GRANT SELECT ON audio_store.* TO 'user'@'localhost';
   FLUSH PRIVILEGES;
   ```

### Ошибка: "Database does not exist"

**Решение:**
1. Проверьте имя базы данных в `DATABASE_URL`
2. Убедитесь, что база данных создана:
   ```sql
   CREATE DATABASE IF NOT EXISTS audio_store;
   ```

### Бэкап создан, но файл пуст

**Причины:**
- База данных пуста
- Ошибка при выполнении mysqldump
- Проблемы с правами доступа

**Решение:**
1. Проверьте логи скрипта
2. Выполните mysqldump вручную для диагностики:
   ```bash
   mysqldump -u user -p audio_store > test_backup.sql
   ```

## Рекомендации для production

### 1. Частота бэкапов

- **Ежедневные бэкапы** - для активных систем
- **Еженедельные бэкапы** - для систем с низкой активностью
- **Перед важными обновлениями** - всегда создавайте бэкап

### 2. Период хранения

- **Минимум 30 дней** - для production окружения
- **90 дней** - для критичных систем
- **Архивирование** - старые бэкапы можно архивировать и хранить отдельно

### 3. Безопасность

- **Храните бэкапы в защищенном месте**
- **Шифруйте бэкапы** с конфиденциальными данными
- **Регулярно проверяйте** возможность восстановления из бэкапа

### 4. Мониторинг

- Настройте уведомления об ошибках бэкапа
- Регулярно проверяйте размер и количество бэкапов
- Тестируйте восстановление из бэкапа

## Дополнительные ресурсы

- [MySQL Backup and Recovery](https://dev.mysql.com/doc/refman/8.0/en/backup-and-recovery.html)
- [mysqldump Documentation](https://dev.mysql.com/doc/refman/8.0/en/mysqldump.html)
- [DEPLOYMENT_CHECKLIST.md](../DEPLOYMENT_CHECKLIST.md) - чеклист деплоя

## История изменений

- **2025-11-05:** Создан скрипт резервного копирования `backup_db.py`
- **2025-11-05:** Добавлена автоматическая очистка старых бэкапов
- **2025-11-05:** Добавлена поддержка настройки директории и периода хранения

