# 🔄 Инструкция по перезапуску prompts-manager

## Проблема
Эндпоинт `/api/v1/prompts/{prompt_name}/reset` возвращал 404 ошибку из-за порядка обработки маршрутов в FastAPI.

## Решение
Маршрут `/reset` перемещен ПЕРЕД более общим маршрутом `/{prompt_name}`, чтобы он обрабатывался первым.

## Как перезапустить сервис

### Вариант 1: Если запущен через start_services_final.py
1. Остановите сервис (Ctrl+C в терминале)
2. Запустите снова:
   ```bash
   python start_services_final.py
   ```

### Вариант 2: Если запущен отдельно
1. Остановите процесс prompts-manager
2. Запустите снова:
   ```bash
   start_prompts_manager.bat
   ```
   или
   ```bash
   cd services/prompts-manager
   python -c "from main import app; import uvicorn; uvicorn.run(app, host='127.0.0.1', port=8007, reload=True)"
   ```

### Вариант 3: Автоматический перезапуск (если используете uvicorn с reload=True)
- Если в команде запуска был `reload=True`, сервис перезагрузится автоматически
- Иначе нужно перезапустить вручную

## Проверка
После перезапуска проверьте, что эндпоинт работает:
```bash
curl -X POST http://127.0.0.1:8007/api/v1/prompts/description_prompt/reset -H "Content-Type: application/json"
```

Или откройте Swagger UI:
```
http://127.0.0.1:8007/docs
```

