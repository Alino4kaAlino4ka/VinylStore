# Сводка по портам и конфигурации

## ✅ Согласованные порты сервисов

| Сервис | Порт | URL | Конфигурация |
|--------|------|-----|--------------|
| **catalog** | 8000 | http://localhost:8000 | ✅ config.env, services/catalog/main.py |
| **auth** | 8001 | http://localhost:8001 | ✅ config.env, services/auth/main.py |
| **orders** | 8002 | http://localhost:8002 | ✅ config.env, services/orders/main.py |
| **users** | 8003 | http://localhost:8003 | ✅ config.env, services/users/main.py |
| **recommender** | 8004 | http://localhost:8004 | ✅ config.env, services/recommender/main.py |
| **cart** | 8005 | http://localhost:8005 | ✅ config.env, services/cart/main.py |
| **prompts-manager** | 8007 | http://localhost:8007 | ✅ config.env, services/prompts-manager/main.py |

## 🔗 Межсервисные зависимости

### Orders Service (8002)
- Использует **catalog** (8000) для получения информации о товарах
- Использует **auth** (8001) для получения информации о пользователе (опционально)
- Отправляет уведомления в **Telegram** при создании заказа

### Recommender Service (8004)
- Использует **prompts-manager** (8007) для получения промптов
- Использует **catalog** (8000) для получения каталога пластинок

### Cart Service (8005)
- Использует **catalog** (8000) для получения информации о товарах

## 📁 Конфигурационные файлы

### Backend (Python)
- `config.env` - основной файл конфигурации с портами и URL сервисов
- `start_services_final.py` - скрипт запуска всех сервисов с правильными портами

### Frontend (JavaScript)
- `src/config/api-config.js` - единый файл конфигурации API для всех frontend компонентов
- Все скрипты используют `window.API_CONFIG` с fallback на localhost

## 🔄 Унификация

### ✅ Выполнено:
1. Все порты согласованы в `config.env`
2. Все сервисы используют правильные порты в `main.py`
3. Создан единый файл `src/config/api-config.js` для frontend
4. Все frontend скрипты обновлены для использования единой конфигурации
5. Унифицировано использование `localhost` вместо `127.0.0.1` в frontend
6. Все HTML файлы подключены к `api-config.js`

### 📝 Заметки:
- Frontend использует `localhost` для единообразия
- Backend использует `127.0.0.1` для межсервисных вызовов
- Все сервисы должны быть запущены через `start_services_final.py` для правильной загрузки конфигурации

## 🚀 Запуск

```bash
python start_services_final.py
```

Все сервисы запускаются в правильном порядке с правильными портами.

