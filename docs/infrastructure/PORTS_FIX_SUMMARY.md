# Исправления портов и конфигурации

## ✅ Исправленные проблемы

### 1. Дублирование API_CONFIG
**Проблема:** `cart.js` пытался объявить `const API_CONFIG`, когда он уже был объявлен в `api-config.js`

**Решение:**
- Изменен `api-config.js` для правильного экспорта в `window.API_CONFIG`
- Изменен `cart.js` для проверки существования перед объявлением

### 2. Согласование портов

Все порты согласованы между:
- `config.env` - конфигурация портов
- `start_services_final.py` - скрипт запуска
- `src/config/api-config.js` - frontend конфигурация
- `PORTS_CONFIG.md` - документация

## 📋 Текущая конфигурация портов

| Сервис | Порт | Статус | Описание |
|--------|------|--------|----------|
| **catalog** | 8000 | ✅ | Каталог товаров |
| **auth** | 8001 | ✅ | Аутентификация |
| **orders** | 8002 | ✅ | Заказы (с Telegram уведомлениями) |
| **users** | 8003 | ✅ | Пользователи |
| **recommender** | 8004 | ✅ | AI-рекомендации |
| **cart** | 8005 | ✅ | Корзина |
| **prompts-manager** | 8007 | ✅ | Управление промптами |

## 🔗 Порядок запуска сервисов

В `start_services_final.py` сервисы запускаются в правильном порядке:

1. **catalog** (8000) - основа для других сервисов
2. **auth** (8001) - для аутентификации
3. **orders** (8002) - использует catalog и auth
4. **users** (8003)
5. **prompts-manager** (8007) - должен быть до recommender
6. **recommender** (8004) - использует prompts-manager и catalog
7. **cart** (8005) - использует catalog

## 📁 Файлы конфигурации

### Backend
- `config.env` - все порты и URL сервисов
- `start_services_final.py` - запуск всех сервисов

### Frontend
- `src/config/api-config.js` - единая конфигурация API
- Все HTML файлы подключают `api-config.js` первым
- Все скрипты используют `window.API_CONFIG` с fallback

## 🚀 Запуск

```bash
python start_services_final.py
```

Все сервисы запустятся автоматически в правильном порядке.

## ⚠️ Важно

1. **Catalog service (8000) обязателен** для работы orders service
   - Orders service получает информацию о товарах из catalog
   - Без catalog service информация о товарах будет неполной

2. **Prompts-manager (8007) должен запускаться до recommender (8004)**
   - Recommender использует промпты из prompts-manager

3. **Все сервисы должны быть доступны на localhost**
   - Frontend использует `localhost`
   - Backend использует `127.0.0.1`

