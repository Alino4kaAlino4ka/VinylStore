# Пример конфигурационного файла
# Скопируйте этот файл в config.env и заполните своими значениями

# API Keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# JWT Security
# ВАЖНО: Для production сгенерируйте новый безопасный ключ!
# Используйте команду: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-super-secret-key-that-is-long-and-random-CHANGE-IN-PRODUCTION

# Database Configuration
# Для SQLite (development):
DATABASE_URL=sqlite:///./audio_store.db

# Для PostgreSQL (production):
# DATABASE_URL=postgresql://user:password@localhost:5432/audio_store

# Service Ports
CATALOG_PORT=8000
AUTH_PORT=8001
ORDERS_PORT=8010
USERS_PORT=8011
RECOMMENDER_PORT=8012
CART_PORT=8005
PROMPTS_MANAGER_PORT=8007

# Service URLs (для межсервисных вызовов)
CATALOG_SERVICE_URL=http://127.0.0.1:8000
AUTH_SERVICE_URL=http://127.0.0.1:8001
ORDERS_SERVICE_URL=http://127.0.0.1:8010
USERS_SERVICE_URL=http://127.0.0.1:8011
RECOMMENDER_SERVICE_URL=http://127.0.0.1:8012
CART_SERVICE_URL=http://127.0.0.1:8005
PROMPTS_MANAGER_SERVICE_URL=http://127.0.0.1:8007

# CORS Configuration
# Для development разрешены все источники, для production укажите конкретные домены
# Пример: ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com
ALLOWED_ORIGINS=*

# JWT Token Settings
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Environment
# Установите в 'production' для production окружения
ENVIRONMENT=development

# Admin Credentials
# ВАЖНО: Для production измените дефолтные значения!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Telegram Configuration
# Для получения токена бота: https://t.me/BotFather
# Для получения chat_id: напишите боту @userinfobot или используйте @getidsbot
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# Email Configuration (SMTP)
# Для Gmail используйте: smtp.gmail.com:587
# Пароль приложения: получите в настройках Google Account
# ВАЖНО: Укажите ваш Gmail адрес в SMTP_USERNAME и EMAIL_FROM
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password_here
EMAIL_FROM=your_email@gmail.com
EMAIL_COPY_TO=your_email@gmail.com

