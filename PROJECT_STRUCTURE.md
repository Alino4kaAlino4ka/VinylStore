# Структура проекта

## 📁 Обзор организации файлов

Проект реорганизован для лучшей структурированности и удобства деплоя.

## 🗂️ Основные директории

```
project_root/
├── config/                    # Конфигурационные файлы
│   └── config.env.example     # Пример конфигурации (без секретов)
│
├── scripts/                   # Все скрипты и утилиты
│   ├── launch/               # Скрипты запуска сервисов
│   │   ├── start_all_services.py  # Основной скрипт запуска всех сервисов
│   │   ├── start_*.py        # Другие скрипты запуска
│   │   └── *.bat             # Batch файлы для Windows
│   │
│   ├── utils/                # Утилиты для работы с проектом
│   │   ├── db/               # Утилиты для работы с БД
│   │   │   ├── init_db.py
│   │   │   ├── seed_db.py
│   │   │   ├── migrate_sqlite_to_mysql.py
│   │   │   ├── setup_mysql_auto.py
│   │   │   ├── fix_mysql_setup.py
│   │   │   └── create_default_prompts.py
│   │   │
│   │   ├── admin/            # Административные утилиты
│   │   │   ├── add_test_user.py
│   │   │   ├── reset_user_password.py
│   │   │   ├── update_user.py
│   │   │   └── reset_description_prompt.py
│   │   │
│   │   └── testing/          # Тестовые утилиты
│   │       ├── test_telegram.py
│   │       ├── test_order_telegram.py
│   │       └── get_telegram_chat_id.py
│   │
│   └── restart/              # Скрипты перезапуска сервисов
│       ├── restart_catalog_service.py
│       ├── restart_orders_service.py
│       └── stop_orders_service.py
│
├── services/                  # Микросервисы бэкенда
│   ├── catalog/              # Сервис каталога (порт 8000)
│   ├── auth/                 # Сервис авторизации (порт 8001)
│   ├── orders/               # Сервис заказов (порт 8010)
│   ├── users/                # Сервис пользователей (порт 8011)
│   ├── recommender/          # Сервис рекомендаций (порт 8012)
│   ├── cart/                 # Сервис корзины (порт 8005)
│   └── prompts-manager/      # Сервис управления промптами (порт 8007)
│
├── src/                       # Фронтенд приложение
│   ├── admin/                # Админ-панель
│   ├── scripts/             # JavaScript модули
│   ├── styles/              # CSS стили
│   └── *.html               # HTML страницы
│
├── database/                  # Модуль базы данных
│   ├── connection.py         # Подключение к БД
│   ├── models.py            # SQLAlchemy модели
│   └── ...
│
├── tests/                     # Тесты
│   ├── test_*.py            # Python тесты
│   └── test_*.html          # HTML тесты
│
├── docs/                      # Документация
│   └── *.md                  # Markdown файлы документации
│
├── logs/                      # Логи сервисов
│   └── *_stdout.log, *_stderr.log
│
├── config.env                 # Конфигурация с секретами (НЕ в Git!)
├── requirements.txt          # Python зависимости
├── README.md                  # Основная документация
└── PROJECT_STRUCTURE.md       # Этот файл
```

## 🔐 Безопасность

### Файлы с секретами

**ВАЖНО**: Следующие файлы содержат секретные данные и **НЕ должны** попадать в Git:

- `config.env` - основной файл с секретами (токены, пароли, API ключи)
- Все файлы `*.db` - базы данных (может содержать чувствительные данные)

### Проверка перед коммитом

```bash
# Убедитесь, что config.env не в Git
git status
# config.env НЕ должен быть в списке измененных файлов

# Проверьте .gitignore
cat .gitignore | grep config.env
# Должно вывести: config.env
```

### Настройка для нового окружения

1. Скопируйте пример конфигурации:
   ```bash
   cp config/config.env.example config.env
   ```

2. Заполните секретные данные в `config.env`

3. Проверьте, что файл добавлен в `.gitignore`

## 🚀 Запуск проекта

### Основной скрипт запуска

```bash
# Запуск всех сервисов
python scripts/launch/start_all_services.py

# Или через batch файл (Windows)
scripts/launch/start_all_services.bat
```

### Утилиты

```bash
# Инициализация БД
python scripts/utils/db/init_db.py

# Заполнение тестовыми данными
python scripts/utils/db/seed_db.py

# Добавление тестового пользователя
python scripts/utils/admin/add_test_user.py
```

## 📝 Изменения по сравнению со старой структурой

### Перемещенные файлы

- ✅ Все скрипты запуска (`start_*.py`, `start_*.bat`) → `scripts/launch/`
- ✅ Утилиты БД → `scripts/utils/db/`
- ✅ Административные утилиты → `scripts/utils/admin/`
- ✅ Тестовые утилиты → `scripts/utils/testing/`
- ✅ Скрипты перезапуска → `scripts/restart/`

### Удаленные файлы

- ❌ `admin.html` (корневой) - дубликат, используется `src/admin/admin.html`
- ❌ `admin/` (корневая директория) - дубликат, используется `src/admin/`

### Обновленные пути

Все скрипты автоматически находят `config.env` в корне проекта, независимо от их расположения.

## 🔄 Обратная совместимость

Все пути к `config.env` обновлены для работы из новой структуры. Скрипты автоматически находят конфигурационный файл в корне проекта.

## 📚 Дополнительная документация

- [README.md](README.md) - основная документация проекта
- [SECURITY.md](SECURITY.md) - подробная информация о безопасности
- [docs/](docs/) - дополнительная документация

